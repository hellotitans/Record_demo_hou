# P6 前端（React）— 阶段完成报告

> 日期：2026-09-01 ｜ 阶段目标：把 P4 的 SSE 协议变成可实时观看的辩论界面
> 纪律：每阶段单一目标、清晰边界、**独立测试门禁**；中途逐模块验证

## 一、做了什么

在后端 P0–P5 已验证的基础上，完成 **P6 前端**阶段：一个实时消费辩论 SSE 流的 React 界面。纯前端，未改后端一行代码。

技术栈（用户选定）：Vite + React 19 + TypeScript + Tailwind + Framer Motion；立绘用 CSS/SVG 占位头像；决策卡片长图用 `html-to-image` 的 `toPng`。

核心模块：
- `web/src/lib/sse.ts` — `SSEParser`（跨块缓冲、坏帧只丢不崩）+ `streamDebate()`（`fetch + ReadableStream` 消费 POST SSE）
- `web/src/components/Avatar.tsx` — 数据派/生活派 SVG 占位头像 + 发言呼吸光环
- `web/src/components/DebateStream.tsx` — 按轮重排的聊天流 + 打字机光标
- `web/src/components/ModeratorNote.tsx` — 紫色分歧挑明卡
- `web/src/components/DecisionCard.tsx` — 决策卡片长图（含假设列表，对接下一子阶段临界点计算器）
- `web/src/App.tsx` — 表单 + 状态机 + 函数式逐帧 `setState`

## 二、中途修复的 3 处门禁失败（用户"中途测试"纪律落地）

1. **`App.tsx` 未使用 state**：`assumptions` 声明后从未读取（假设结论随 `done` 帧进入 `result.assumptions`，单一数据源）→ 删除该 state、`setAssumptions` 调用与 `assumption` 帧分支。
2. **`DebateStream.test.tsx` 文本拆分**：主持人笔记被渲染成 `<span>未解分歧：</span>` + `价格敏感度` 两个节点，`getByText('未解分歧：价格敏感度')` 找不到 → 改成分两段断言。
3. **`App.test.tsx` 多匹配**：辩论流与决策卡片都渲染同一发言文本，`findByText` 命中多个 → 改用 `findAllByText(...).length > 0`。

## 三、门禁结果（全绿）

```
npm run typecheck   → tsc --noEmit 零错误
npm test            → 12 passed（3 文件）
                        sse.test.ts 5 / DebateStream.test.tsx 4 / App.test.tsx 3
npm run build       → tsc --noEmit && vite build：451 模块转译，dist/ 产出
```

## 四、后端真机联调（最关键的一跳）

起 Go 后端（Mock 模式，未配 API Key，零成本），`POST /api/debate` 收到的**真实 SSE**：
`role_assign×1 → turn_start/delta/turn_end×8 → moderator×3 → done×1`

帧类型与 `SSEParser` 的 `FrameKind` 契约**完全吻合**（解析器以 `data.kind` 为权威帧类型）—— 前端解析器 ↔ 后端线格式两端对齐，不是"我以为能对上"。

## 五、结论与下一步

- ✅ P6 前端阶段完成，门禁全绿，已登记进《开发计划-分阶段实施.md》P6 小节。
- ⬜ P6+ 剩余子阶段：临界点计算器（消费 `result.assumptions`）、品类模板库、决策档案回访。
- ⬜ 后端仍待补验证短板：`-race`（需 gcc/cgo）、Linux SIGTERM 优雅关闭。

## 六、运行方式

```bash
# 前端（开发）
cd web && npm install && npm run dev      # /api 代理到 http://127.0.0.1:8080

# 后端（Mock，零成本）
cd server
export GOROOT="C:/Users/Arina/.workbuddy/binaries/go"
export GOPATH="C:/Users/Arina/WorkBuddy/2026-09-01-12-41-31/server/gopath"
export GOCACHE="C:/Users/Arina/WorkBuddy/2026-09-01-12-41-31/server/gocache"
./bin/server.exe                            # 默认 :8080
```

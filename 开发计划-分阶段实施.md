# 决策辩论产品 — 分阶段开发计划

> 目标：把"数据派 vs 生活派"四段式辩论引擎从零做到可运行、可验证、可观测。
> 原则：**每个阶段有单一目标、清晰边界、独立验收测试**。上一阶段没过测试，不进入下一阶段。

---

## 阶段总览

| 阶段 | 名称 | 单一目标 | 状态 |
|---|---|---|---|
| **P0** | 埋点底座 | 事件可采集、可落盘、永不阻塞主流程 | ✅ 已完成（20 测试 / 89.9% 覆盖） |
| **P1** | 领域模型与提示词 | 数据结构自校验，四段式提示词可复现 | ✅ 已完成（16 测试 / 85.8% 覆盖） |
| **P2** | 模型抽象层 | 不联网、不花钱跑通全流程；混合模型路由可验证 | ✅ 已完成（13 测试 / 84.8% 覆盖） |
| **P3** | 辩论编排引擎 | 四段式流程 + 主持人 + 优雅降级，Mock 下跑完整场 | ✅ 已完成（6 测试 / 93.5% 覆盖） |
| **P4** | HTTP / SSE 交付层 | 端到端可请求，帧序列正确，埋点自动上报 | ✅ 已完成（httpsrv 85.6% 覆盖） |
| **P5** | 可执行入口与冒烟 | 一条命令起服务，真实 HTTP 请求跑通整场 | ✅ 已完成（二进制 + 真机冒烟） |
| **P6** | 前端（React） | 实时辩论流 UI：SSE 消费 + 立绘 + 打字机 + 决策卡片长图 | ✅ 已完成（typecheck/test/build 全绿） |
| **P6·a** | 临界点计算器 | 把辩论假设变成可拖拽的"结论翻转沙盘"（纯前端，消费 result.assumptions） | ✅ 已完成（前端门禁全绿） |
| **P6·b** | 品类模板库 | 6–10 个高频品类的决策维度预置，点选预填 + 可编辑权重 | ✅ 已完成（前端门禁全绿） |
| **P6·c** | 决策档案 + 回访 | localStorage 存每场结论+假设，超 3 个月弹"你后悔吗"回访 | ✅ 已完成（前端门禁全绿） |
| **P7·a** | 后端 `/api/decisions` | 决策档案搬到服务端：文件 JSON 存储 + REST 接口，跨设备可读同一份档案 | ✅ 已完成（Go 门禁全绿，store 88.7% / httpsrv 86.4%） |
| **P7·b** | 档案跨设备同步 | 前端改为"远端权威 + 本地兜底"，后端不可用时功能不残废 | ✅ 已完成（前端门禁全绿，94 测试） |
| **P8·a** | 后端统计聚合 | store 加 Category 字段 + `Stats()` 聚合 + `GET /api/decisions/stats` | ✅ 已完成（Go 门禁全绿，store 92.2% / httpsrv 86.8%） |
| **P8·b** | 后悔率看板 | 前端看板：总览卡 / 品类排行 / 月度趋势 / 后悔列表 | ✅ 已完成（前端门禁全绿，112 测试） |
| **V** | 验证短板 | `-race` 竞态 / SIGTERM 优雅关闭真机验证 | ⬜ 本机环境不满足（无 gcc；Windows 无 POSIX 信号） |

---

## 阶段边界（关键设计约束）

```
debate  ──▶ llm  ──▶ orchestrator  ──▶ httpsrv  ──▶ cmd/server
  │                                        │
  └────────────── telemetry ──────────────┘
```

**依赖方向严格单向，禁止反向依赖。**

| 包 | 职责 | 禁止做的事 |
|---|---|---|
| `debate` | 领域模型 + 提示词 | 任何 I/O、任何对上层的引用 |
| `llm` | 模型调用抽象 | 引用 `debate`（保持通用） |
| `orchestrator` | 流程编排 | 直接写 HTTP、直接写埋点存储 |
| `httpsrv` | 协议层 | 内含业务逻辑 |
| `telemetry` | 采集 | 被主流程阻塞（`Emit` 必须永不阻塞） |

**这样做的理由**：每一层都能独立测试。`orchestrator` 用 Mock 模型验证，`httpsrv` 用 Mock 编排器验证 —— 阶段之间互不干扰，某个阶段改坏了不会污染其他阶段的测试。

---

## P0 — 埋点底座 ✅ 已完成

**目标**：事件可采集、可落盘、绝不阻塞主流程。

**边界**：只管"采和存"，不管"分析"。不引用任何其他业务包。

**验收标准**：
- [x] `go vet` / `go build` 通过
- [x] 20 个测试全过，覆盖率 89.9%
- [x] `-count=10` 重复运行稳定（无时序 flake）
- [x] `Emit` 缓冲区满时直接丢弃并计数，不阻塞
- [x] 所有事件带 `zone` 字段（`decision` / `execution`）—— 商业内容时序隔离在数据层的落地

**遗留**：`-race` 竞态检测跑不了（本机无 gcc/cgo）。装 gcc 后需补跑。

---

## P1 — 领域模型与提示词 🎯 目标

**目标**：数据结构能自我校验，四段式提示词可复现、可断言。

**为什么先做这个**：提示词是本产品的核心资产，把它和流程控制分开，才能在不跑模型的前提下审查和迭代它。

**验收标准**：
- [x] `Dilemma.Validate()` 覆盖所有非法输入（空 ID、空选项、两个选项同 ID）
- [x] `RoundNo.FirstSpeaker()` 对 R1 返回 `ok=false`（R1 是并行的，先手无意义）
- [x] R2–R4 先手交替，验证消除先手优势的逻辑
- [x] 四段式提示词每轮包含**不同的**战术指令（用字符串断言证明"每轮更深"不是空话）
- [x] 提示词中明确写入"禁止说你说得对"（防 LLM 趋同）

**测试文件**：`internal/debate/debate_test.go`、`internal/debate/prompt_test.go`

---

## P2 — 模型抽象层 🎯 目标

**目标**：不联网、不花钱跑通全流程；混合模型路由可被验证。

**验收标准**：
- [x] `Router.Model()` 未配置档位时能回退，永不返回空模型名
- [x] `MockClient` 支持分块流式 + 延迟，能真实模拟取消与超时
- [x] `MockClient.Handler` 并发安全（R1 两个角色并行调用）
- [x] `MockClient.Calls()` 可断言模型选择 —— 验证"R1 便宜模型、R2–R4 强模型"
- [x] OpenAI 客户端能正确解析 SSE 流（含 `[DONE]` 终止标记）
- [x] OpenAI 客户端能在 ctx 取消时立即退出

**测试文件**：`internal/llm/llm_test.go`、`internal/llm/mock_test.go`、`internal/llm/openai_test.go`

---

## P3 — 辩论编排引擎 🎯 目标

**目标**：四段式流程 + 主持人 + 优雅降级，Mock 模型下跑完整场。

**关键设计判断**：**主体串行，只有 R1 并行。**
质询轮要求 B 点名反驳 A 的发言，存在数据依赖 —— 并行反而会让 B 看不到 A 说了什么。
唯一的真实并行点是 R1 立论（该轮明确禁止提及对方，两边互不依赖）。**不为了用并发而用并发。**

**降级策略（本阶段最重要的部分）**：

| 失败点 | 行为 | 理由 |
|---|---|---|
| 角色分配解析失败 | 降级为确定性分配 + 诚实理由 | 绝不让一场辩论卡在这一步 |
| 主持人输出不合法 | 降级为空笔记，辩论继续 | 让跑了几十秒的辩论因 JSON 少个括号而全盘失败，是把工程问题转嫁给用户 |
| 某一方发言失败 | 保留已完成部分，返回非 nil result | 用户等了 60 秒，半场辩论也比空白页好 |
| 下游（前端）断开 | 立即 `cancel()`，停止后续调用 | 用户关了页面，不该继续为没人看的辩论付钱 |

**验收标准**：
- [x] Mock 模型下跑完整场，产出 8 个 Turn（4 轮 × 2 方）
- [x] R1 两方并行（总耗时 < 串行）
- [x] R2–R4 串行，后手能看到先手内容（断言提示词包含对手发言）
- [x] 主持人笔记中的 `Unresolved` 被喂进下一轮提示词 —— 这是"深度递增"的传动轴
- [x] R4 后不再跑主持人，改为提取关键假设
- [x] 模型调用失败时，仍返回已完成轮次（非 nil）
- [x] 下游断开立即停止后续调用（断言调用次数减少）
- [x] **修复**：模型调用返回 error 时不得解引用 `resp`（3 处 nil deref 隐患）

**测试文件**：`internal/orchestrator/orchestrator_test.go`

---

## P4 — HTTP / SSE 交付层 🎯 目标

**目标**：端到端可请求，帧序列正确，埋点自动上报。

**验收标准**：
- [x] 编译通过（当前缺 `context` import，缺 3 个埋点符号）
- [x] `httptest` 下请求一次，能收完整帧序列
- [x] 帧顺序正确：`role_assign → (turn_start → delta* → turn_end) × 8 → moderator × 3 → assumption* → done`
- [x] 心跳帧按间隔插入（辩论中途几十秒无数据，没有心跳会被反向代理掐断）
- [x] 请求体超限返回 413
- [x] 非法辩题返回 400
- [x] 埋点在辩论过程中自动上报 `debate_started` / `round_delivered` / `llm_call_completed` / `debate_cost_total`
- [x] 埋点失败绝不影响主流程

**测试文件**：`internal/httpsrv/handler_test.go`、`internal/httpsrv/sse_test.go`

---

## P5 — 可执行入口与冒烟 🎯 目标

**目标**：一条命令起服务，真实 HTTP 请求跑通整场，埋点落到文件。

**验收标准**：
- [x] `cmd/server` 可编译
- [x] 配置全部走环境变量（API Key 绝不进代码）
- [x] 未配置 API Key 时用 Mock 模型启动，本地零成本跑通
- [x] `curl` 一次请求能收到完整 SSE 流
- [x] 埋点落盘为 JSON Lines，内容可查
- [x] 优雅关闭（SIGINT 时刷完剩余埋点再退出）

---

## P6 — 前端（React）✅ 已完成

**目标**：把 P4 的 SSE 协议变成能实时观看的辩论界面——两个角色立绘、打字机流式发言、主持人分歧挑明卡、结尾决策卡片长图。纯前端，不改后端一行代码。

**技术栈（用户选定）**：Vite + React 19 + TypeScript + Tailwind + Framer Motion；立绘用 CSS/SVG 占位头像（不引入图片资源）；决策卡片长图导出用 `html-to-image` 的 `toPng`。

**关键设计判断**：
- 浏览器原生 `EventSource` 只支持 GET，而 `/api/debate` 是 POST，所以用 `fetch + ReadableStream` 消费；`SSEParser` 跨块保留缓冲，损坏帧只丢不崩（`data` JSON 解析失败返回 null）。
- 所有帧处理走函数式 `setState`（避免高频流式回调里的旧闭包），`live` 帧增量累积实现打字机。
- `assumption` 帧不单独建 state，最终假设随 `done` 帧的 `result.assumptions` 进入决策卡片——单一数据源，避免重复与漂移。

**验收标准（门禁全绿，2026-09-01）**：
- [x] `npm run typecheck`（`tsc --noEmit`）零错误
- [x] `npm test`：**12 passed**（3 文件）—— `sse.test.ts` 5（多帧/跨块拆分/心跳忽略/损坏帧容错/flush 兜底）、`DebateStream.test.tsx` 4、`App.test.tsx` 3（mock SSE 驱动端到端）
- [x] `npm run build`（`tsc --noEmit && vite build`）：451 模块转译，`dist/` 产出
- [x] **后端真机联调**：起 Go 后端（Mock 模式，零成本），`POST /api/debate` 收到的真实 SSE 帧序列 `role_assign×1 → turn_start/delta/turn_end×8 → moderator×3 → done×1`，与 `SSEParser` 的 `FrameKind` 契约完全吻合（解析器取 `data.kind` 为权威帧类型）

**文件**：`web/src/{types.ts, lib/sse.ts, App.tsx, components/{Avatar,ModeratorNote,DebateStream,DecisionCard}.tsx}`，测试 `*.test.ts(x)`。

**仍遗留（下一子阶段）**：决策档案回访（P6·c）；`-race` 与 Linux 优雅关闭需在合适环境补验证。临界点计算器（P6·a）、品类模板库（P6·b）已在本会话完成（见下）。

---

## P6·a — 临界点计算器 ✅ 已完成

**目标**：把 P6 决策卡片里"辩论暴露的关键假设"变成可拖拽的**临界点沙盘**——每个变量一根滑块，用户把假设拖成现实，实时看到哪一方论证先崩、结论何时翻转。纯前端，消费 `result.assumptions`，不改后端一行代码。

**为什么纯前端**：假设已在 `done` 帧随 `result.assumptions` 送到前端（P6 决策卡片已渲染），计算只是"按变量取值套 operator 判断哪方成立"。交互式拖拽天然是客户端行为，无需新增后端端点，阶段边界最干净。

**关键设计判断**：
- 算子语义（对齐 `prompt.go` 的假设提取约定）：假设即"结论成立的前提线"，`realValue OP threshold` 为真则该方论证成立；越过 `value` 即崩溃。临界值就是 `value` 本身。
- 每方最多一条假设、可能落在**不同变量**上 → 构成二维沙盘。`computeWinner` 对双方各自"全部假设是否成立"取与：都成立=平局，仅一方成立=该方胜，都不成立=双双崩塌。
- 滑块默认放在该变量各阈值**中点**（同变量双方各一条时即中界），初始即"平局"，诚实表达"辩论本身不替你做决定，得你输入现实"。
- 纯函数核心 `lib/critical.ts` 不碰 DOM、不发请求，UI 只当展示层——可单测、可当唯一事实源。

**验收标准（门禁全绿，2026-09-01）**：
- [x] `npm run typecheck` 零错误
- [x] `npm test`：**32 passed**（5 文件）—— 新增 `critical.test.ts` 16（四种算子/边界/平局/翻转/both-broken/同变量共享/range 合法性/flipHint 措辞）、`CriticalCalculator.test.tsx` 4（默认平局、拖滑块翻转结论、复原回假设线）
- [x] `npm run build`：453 模块转译，`dist/` 产出
- [x] 模块测试验证"拖滑块→结论翻转"：数据派假设 `涨幅>=3%` 拖到 2 → 生活派胜；生活派 `月供<=40%` 拖到 50 → 数据派胜

**文件**：`web/src/lib/critical.ts`（纯算法）、`web/src/components/CriticalCalculator.tsx`（滑块沙盘 + framer-motion 结论卡片交叉淡入）、接入 `web/src/App.tsx`（`done` 区块、决策卡片之下，仅当 `result.assumptions.length>0`）。

**测试踩坑（已修，供复用）**：
- RTL 对 `<input type=range>` 的 `onChange` 监听的是 `input` 事件，`fireEvent.change` 不触发；滑块测试须用 `fireEvent.input`。
- 结论卡片里的胜方标签（数据派/生活派）也会出现在每条假设的静态文案中，`getByText` 会命中多个；断言改用结论卡片里**唯一的 subLine** 文案。
- AnimatePresence 在 jsdom 下退出动画不结束、旧节点常驻，故只用"新文案出现"做断言，不断言旧文案消失。

---

## P6·b — 品类模板库 ✅ 已完成

**目标**：高频两难"开箱即用"——用户点选品类后自动预填问题/选项，并挂上该品类常见的决策维度（带权重）；维度可删、可加、可拖权重。提交时把 `category` + `dimensions` 一起发给后端。纯前端，后端零改动。

**为什么纯前端（边界判断）**：后端 `debate.Dilemma` 早已预留 `Category` / `Dimensions []Dimension` 字段，`prompt.go:305-311` 也已会把"用户确认过的决策维度及权重"渲染进提示词（两派据此调论证重点、但不得替用户改权重）。所以前端把维度发过去就是端到端生效，无需新增后端代码，阶段边界最干净。范围经用户确认：纯前端 + 维度可编辑。

**关键设计判断**：
- 数据是单一事实源：`web/src/lib/templates.ts` 内置 10 个品类（数码购买/租房买房/求职/考研/城市/投资/婚恋/医疗/创业/买车），每模板 2 个样例选项 + 2–4 个带权重的维度（`source:"template"`）。
- `DimensionEditor` 受控组件：每条维度可改名、拖权重（0–100%）、删除；底部"添加维度"追加 `source:"user"` 项。`Dimension` 类型与后端 `debate.Dimension` 的 JSON 形状一一对应（key/label/weight/source）。
- `CategoryPicker` 横向可滚动 chips，含"自定义"清空项；选中即预填表单并载入模板维度，选中态用 `aria-pressed` 表达。
- `App.start()` 的 body 用 `Record<string,unknown>` 动态挂 `category`（有值时）与 `dimensions`（非空时）；`reset()` 一并清二者。字段名与后端 `omitempty` 兼容。

**验收标准（门禁全绿，2026-09-01）**：
- [x] `npm run typecheck` 零错误
- [x] `npm test`：**55 passed**（8 文件）—— 新增 `templates.test.ts` 13（品类数 6–10、每模板恰 2 选项、维度≥2、权重∈(0,1]、key 唯一）、`CategoryPicker.test.tsx` 4（渲染全部品类、点击命中模板、自定义→null、aria-pressed）、`DimensionEditor.test.tsx` 5（渲染/删除/添加/拖权重更新/空态）、`App.test.tsx` 增 1（选品类后请求体含 category+dimensions）
- [x] `npm run build`：456 模块转译，`dist/` 产出
- [x] **后端真机联调**：起 Go 后端（Mock 模式），`POST /api/debate` 带 `category:"phone"` + 两条 `dimensions` → 完整帧序列正常产出（`role_assign×1 → turn_start/delta/turn_end×8 → moderator×3 → done×1`），**无 error 帧**，证明后端解析维度字段无误、维度进提示词不回传（符合 `Result` 结构）

**文件**：`web/src/lib/templates.ts`（模板数据）、`web/src/components/CategoryPicker.tsx`、`web/src/components/DimensionEditor.tsx`、`web/src/types.ts`（新增 `Dimension` / `CategoryTemplate`）、接入 `web/src/App.tsx`。

**测试踩坑（已修，供复用）**：
- `DimensionEditor` 是受控组件，测试里若传 `onChange={() => {}}` 空操作，拖滑块不会改显示值；需用带 `useState` 的 Harness 包一层才能验证"拖权重→显示更新"。
- `App.test.tsx` 里 `vi.mocked(streamDebate).mock.calls` 在同文件测试间**累积**，断言请求体必须用 `calls[calls.length - 1]` 取最后一次，否则会读到上一个测试的 body。（`.at(-1)` 因 tsconfig 的 lib 未开 es2022 在 typecheck 阶段报错，故用下标。）

---

## P6·c — 决策档案 + 回访 ✅ 已完成

**目标**：每场辩论的结论与关键假设自动落库（localStorage），首页展示决策档案；对超过 3 个月的决策弹出"三个月过去了，你后悔吗？"回访卡，用户记录后悔/不后悔。纯前端，后端零改动。

**为什么纯前端（边界判断）**：回访是"时间维度上的数据资产"——别的辩论产品抄不走。但主动定时推送需要服务端调度，超出本阶段边界；纯前端用 `createdAt` 时间戳在用户回来时判定是否"该回访"（≥3 个月且无回访结果即弹卡），既拿到核心数据资产，又不引入后端持久化。范围经用户确认：纯前端·存档+回访。

**关键设计判断**：
- `web/src/lib/archive.ts` 是单一持久化层：`loadArchive`（容错返回 `[]`）、`saveArchive`、`addDecision`（按 `id` 去重、最新在前）、`markFollowup`（置 `regret`+`answeredAt`）、`isDueForFollowup(rec, months=3, now=Date.now())`（已回访→false；未回访且满 3 个月→true；`now` 可注入便于单测）。
- `DecisionRecord` 类型（含可选 `followup`）与后端无关，是纯前端档案模型；`addDecision` 在 `App` 的 `done` 帧里由 `result.dilemma_id` + 当前 `question`/`labels` 组装。
- `DecisionArchive` 在 idle 视图表单下方渲染：每条显示问题/日期(YYYY-MM-DD)/选项/假设条数；超期未回访内联 `<FollowUp>`，已回访显示"已回访：后悔/不后悔·日期"，近期只展示不弹。`reset()` 不清 archive（跨场持久）。
- 写入/读取全部 try/catch 降级，存档失败绝不影响辩论主流程。

**验收标准（门禁全绿，2026-09-01）**：
- [x] `npm run typecheck` 零错误
- [x] `npm test`：**72 passed**（11 文件）—— 新增 `archive.test.ts` 9（round-trip/去重/markFollowup/isDueForFollowup 含边界/损坏数据降级）、`FollowUp.test.tsx` 3、`DecisionArchive.test.tsx` 4（空态/超期弹回访并回调/近期不弹/已回访态）、`App.test.tsx` 增 1（done 后 localStorage 可读回该决策记录）
- [x] `npm run build`：459 模块转译，`dist/` 产出

**文件**：`web/src/lib/archive.ts`、`web/src/components/FollowUp.tsx`、`web/src/components/DecisionArchive.tsx`、`web/src/types.ts`（新增 `DecisionRecord`）、接入 `web/src/App.tsx`（`archive` state 挂载即 `loadArchive()`；`done` 帧落库；idle 渲染档案 + 回访回调 `markFollowup`）。

**测试踩坑（已修，供复用）**：
- 单测 `isDueForFollowup` 的"近期"用例曾误写成 `now - 10*MONTH`（实为 10 个月前、已超期），修正为 10 天前；边界阈值用 `>= months*30d` 表示"满 N 个月即到期"。
- `App.test.tsx` 新增落库测试必须给 `h.frames` 设 `done` 帧，否则 mock 不推帧、永远到不了 `done`、断言超时；且 `beforeEach` 加 `localStorage.clear()` 隔离各测试。

---

## P7 — 决策档案跨设备持久化 ✅ 已完成

**目标**：把 P6·c 只存在浏览器本地的决策档案搬到服务端，让同一份"你后悔吗"数据能跨设备访问。分两个子阶段：**P7·a 后端接口**（门禁只跑 Go）、**P7·b 前端接入**（门禁只跑前端），各自边界干净、可独立验证。

**范围确认（用户选定）**：存储用**文件 JSON**（零第三方依赖）、前端读写策略用**远端权威 + 本地兜底**、归属用**单用户无鉴权**（个人工具定位）。

### P7·a — 后端 `/api/decisions`

**关键设计判断**：
- 新增 `internal/store` 包：单文件 JSON，与埋点 JSONL 落盘模式一致，**零第三方依赖**（不引 SQLite/cgo，Windows 本机直接跑）。
- 写入用**临时文件 + `os.Rename` 原子替换**：进程被杀也不会留下半截 JSON；写完清理临时文件（有测试守着）。
- **损坏文件 fail-fast**：启动时 JSON 解析失败直接返回错误、进程退出。宁可让人来修，也不能静默清空用户的历史决策。
- 错误用哨兵值（`ErrInvalidRecord` / `ErrNotFound`），由 `writeStoreError` 集中映射成 400/404/500 —— 业务语义只在一处翻译。
- 路由用 Go 1.22+ 增强 ServeMux（`GET /api/decisions`、`POST /api/decisions/{id}/followup`），方法与通配由 mux 分发，405 自动带 `Allow`。
- `POST /api/decisions` 按 `id` 幂等：首次 201、覆盖 200 —— 前端重试不会长出双胞胎。
- 前端漏传 `createdAt` 时服务端补齐：否则前端 `isDueForFollowup` 会把空串解析成 NaN，变成"永远不弹回访"的静默 bug。
- `store.DecisionRecord` 的 JSON tag 与前端 `web/src/types.ts` 的 `DecisionRecord` 字段一一对应。

**接口**：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/decisions` | 返回 `{"decisions":[...]}`（新→旧；空档案是 `[]` 不是 `null`） |
| POST | `/api/decisions` | 创建/覆盖一条，返回 `{"decision":{...}}`，201 / 200 |
| POST | `/api/decisions/{id}/followup` | body `{"regret":"yes"\|"no"}`，返回 `{"decision":{...}}` |

**验收标准（Go 门禁全绿，2026-09-02）**：
- [x] `go build ./...` / `go vet ./...` / `gofmt -s -l .` 全过
- [x] `go test -cover ./...`：新增 `store` **88.7%**、`httpsrv` **86.4%**（较 P4 的 85.6% 提升），其余包保持原水平（debate 85.8 / llm 84.8 / orchestrator 93.5 / telemetry 89.9）
- [x] `store` 16 测试：round-trip、去重置顶、缺字段拒绝、回访（含覆盖与未知 id）、**重开能读回**（持久化真实性）、并发 50 写入不丢且文件仍可解析、损坏文件 fail-fast、无临时文件残留
- [x] `httpsrv` 新增 14 测试：走**与 cmd/server 一致的真实 mux 路由**，覆盖空列表/创建/幂等/400/413/405、回访往返/404/400、未注册路径，外加 handler 自带 405 分支

**文件**：`server/internal/store/decision.go`（+`decision_test.go`）、`server/internal/httpsrv/decisions.go`（+`decisions_test.go`）、接入 `server/cmd/server/main.go`（新增 `DECISIONS_FILE` 环境变量，默认 `decisions.json`）。

### P7·b — 前端"远端权威 + 本地兜底"

**关键设计判断**：
- 新增 `web/src/lib/decisionsApi.ts`：**所有失败（网络错误/超时/5xx/结构不合法）一律降级为 `null`，绝不抛异常**。档案是锦上添花的数据，它挂了不能让用户在辩论页看到红色报错。
- `archive.ts` 从"纯本地层"升级为"同步层"，保留原本地函数：
  - 读 `hydrateArchive()`：拉远端，成功则以远端为准并**回写本地缓存**；失败退回本地。
  - 写 `pushDecision()` / `pushFollowup()`：**先落本地（乐观，后端没起也不丢）**，再推远端；远端回执用 `upsertLocal` **就地替换**（不改列表顺序，否则"后端确认一下"就把老决策顶到最前，用户看着像凭空冒出一条）。
  - 回访的 `answeredAt` 以**服务端时钟**为准 —— 跨设备场景下只有服务端时钟是大家公认的那一个。
- `App.tsx` 挂载时 `useEffect` 水合档案（`cancelled` 标志防竞态）；`done` 帧调 `pushDecision`；回访调 `pushFollowup`。

**验收标准（前端门禁全绿，2026-09-02）**：
- [x] `npm run typecheck` 零错误
- [x] `npm test`：**94 passed**（12 文件）—— 新增 `decisionsApi.test.ts` 13（URL/方法/body 形状、500/400/404/网络中断/结构不合法全部降级为 null、脏条目过滤、URL 编码、无 fetch 环境）、`archive.test.ts` 增 7（远端成功以远端为准并回写本地、远端失败降级本地、推送失败本地不丢、回访采用服务端时钟、远端确认不乱序）、`App.test.tsx` 增 2（done 后推送到后端、点击回访推送到后端且先落本地）
- [x] `npm run build`：460 模块转译
- [x] **前后端真机联调**：起 Go 后端（8080）+ vite dev（5173），**经 vite proxy** 走 `/api/decisions` 全链路通过 —— 创建 201、回读可见、回访 200 并落盘、404/400/405 经 proxy 仍正确

**文件**：`web/src/lib/decisionsApi.ts`、`web/src/lib/archive.ts`、`web/src/App.tsx`、`web/src/App.test.tsx`。

**测试踩坑（已修，供复用）**：
- `vi.fn(async () => ...)` 未声明参数时，`mock.calls[0][0]` 在 typecheck 阶段会报"Tuple type '[]' of length '0' has no element at index '0'"。修法：显式标注 `vi.fn<typeof fetch>(async () => ...)`。
- `App.test.tsx` 里给档案 API 打 mock 后，`beforeEach` 要用 `vi.clearAllMocks()`（只清调用记录、不重置实现），否则跨测试累积会读到上一个测试的调用。

---

## P8 — 后悔率看板 ✅ 已完成

**目标**：把 P7 存下来的"你后悔吗"回访数据变成一眼能读懂的决策质量视图。分两个子阶段：**P8·a 后端聚合**（门禁只跑 Go）、**P8·b 前端看板**（门禁只跑前端）。

**范围确认（用户选定）**：给 `DecisionRecord` **加 `category` 字段**（支撑按品类拆解）；看板展示**全部四项**——总览数字卡、按品类后悔率排行、月度趋势、最后悔的决策列表。

### P8·a — 后端统计聚合

**关键设计判断**：
- `DecisionRecord` 增加 `Category string`（`json:"category,omitempty"`）。**不做取值白名单**：品类模板定义在前端 `templates.ts`，后端不该被前端的模板清单绑住，新增品类无需改后端。老档案没有该字段，读出即空串归入"未分类"。
- `ComputeStats(items, now)` 做成**不碰锁的纯函数**，单测直接喂数据；`FileStore.Stats()` 只负责加锁调用 —— 与前端 `critical.ts` 同思路。
- **后悔率的分母是"已回访数"，不是"总决策数"**。没回答过的人不能被当成"不后悔"，否则档案越攒越多，后悔率会被稀释成一个好看但没意义的数字。已回访为 0 时返回 0 而不是 NaN（前端靠 `Answered` 区分二者）。
- **小样本的品类照样进排行**。1/1 = 100% 看着吓人，但藏起来才是误导；所以每条都带 `Answered`，前端把样本量摆在比率旁边（"后悔 1/2（2 条）"）。
- **时间解析失败的记录归入"未知"桶而不是丢弃**，各分项之和始终等于 `Total`，看板不会出现"总数对不上"。
- 排序必须**稳定**：map 遍历顺序随机，兜底按品类名排序，否则同一份档案会排出不同顺序、测试无法断言。
- `Regretted` 里的 `Followup` **深拷贝**：它是指针，共享出去的话调用方改一下就能脏到存储内部。

**接口**：`GET /api/decisions/stats` → `{"stats":{total,answered,pending,regretYes,regretNo,regretRate,byCategory[],byMonth[],regretted[]}}`；只读，非 GET 一律 405。

**验收标准（Go 门禁全绿，2026-09-10）**：
- [x] `build` / `vet` / `gofmt -s -l` 全过
- [x] `go test -cover ./...`：`store` **92.2%**（P7 时 88.7%）、`httpsrv` **86.8%**（P7 时 86.4%），其余包不变
- [x] `store` 新增 12 测试：空档案（切片是 `[]` 不是 `nil`）、分母口径、90 天边界（89/90/200 天 + 已回访超期）、坏时间归未知桶、品类排序与稳定性（跑 20 次）、未分类兼容、月度排序含未知桶、后悔列表倒序、Followup 深拷贝、`FileStore.Stats()` 端到端与重开可读回（含 category 落盘）
- [x] `httpsrv` 新增 7 测试：走**与 cmd/server 一致的真实 mux**、空档案、HTTP 往返聚合、pending 计数、category 落盘进统计、405（Allow 含 GET）、未注册路径 404
- [x] **真机冒烟**：起服务造 5 条带品类数据（含 1 条 120 天前未回访）→ `total=5 answered=3 pending=1 regretYes=2 rate=0.6667`，品类排序 `city(1.0) > phone(0.5) > job(0) > rent(0)`，与预期逐项吻合

**文件**：`server/internal/store/stats.go`（+`stats_test.go`）、`decision.go`（加 Category）、`server/internal/httpsrv/stats.go`（+`stats_test.go`）、接入 `cmd/server/main.go`。

### P8·b — 前端后悔率看板

**关键设计判断**：
- **统计只认后端一份，不在前端用本地档案重算**。聚合口径（分母、90 天到期）在 Go 侧有单测守着，前端再实现一遍等于把同一套语义复制成两份，迟早漂移。后端不可用时看板整块不渲染 —— 它是锦上添花，不该反过来成为主流程的负担。
- **比率旁边永远挂着样本量**（"后悔 1/2（2 条）"）。个人工具数据量天然很小，1/1 = 100% 和 40/40 = 100% 完全不是一回事。
- **条形图用纯 CSS 宽度，不引图表库**。最多十来个数据点，图表库带来的是体积和依赖，不是可读性（与项目零第三方依赖的纪律一致）。
- `fetchStats` 遵守 P7 的降级约定：网络失败 / 5xx / 结构不合法一律返回 `null`，且**校验三个数组真的是数组**（看板直接 `.map()`，半截结构会让整块崩掉）。
- **App 落库品类用 ref 抓**。`start()` 一开头就 `reset()` 清掉了 `category` state，而落库发生在几十秒后的 `done` 帧 —— 不先用 `debateCategoryRef` 存下来，落库时拿到的就是 `null`。（这条有专门的测试守着。）

**验收标准（前端门禁全绿，2026-09-10）**：
- [x] `npm run typecheck` 零错误
- [x] `npm test`：**112 passed**（13 文件；P7 时 94）—— 新增 `StatsBoard.test.tsx` 10（null/空档案不渲染、四个总览数字、品类中文名与"未分类"、样本量可见、排序、月度含"未知"、后悔列表、空态）、`decisionsApi.test.ts` 增 5（URL/方法、500、网络中断、5 种坏结构、无 fetch 环境）、`App.test.tsx` 增 3（落库带 category=phone、未选品类时不带该字段、后端返回统计时首页渲染看板）
- [x] `npm run build`：461 模块转译
- [x] **前后端真机联调**：起后端 8080 + vite dev 5173，**经 vite proxy** 打 `/api/decisions/stats` 拿到与落盘数据一致的统计（total=5 / rate=0.6667 / 4 个品类桶 / 4 个月份桶 / 2 条后悔记录）

**文件**：`web/src/types.ts`（DecisionRecord 加 `category?`；新增 `DecisionStats`/`CategoryStat`/`MonthStat`）、`web/src/lib/decisionsApi.ts`（`fetchStats`）、`web/src/components/StatsBoard.tsx`（+测试）、接入 `web/src/App.tsx`。

**测试踩坑（已修，供复用）**：
- 给 `decisionsApi` 的 `vi.mock` 加新导出后，**忘了同步补进 mock 会让所有 App 测试全挂**（报 `No "fetchStats" export is defined on the mock`）。
- `StatsBoard` 里同一个品类名会同时出现在"品类排行"和"最后悔的决策"两处，`getByText` 会命中多个；断言用的测试数据要把 `regretted` 置空或只留一个品类。
- 用 `mockResolvedValue` 改默认实现会**污染后续测试**（`vi.clearAllMocks()` 只清调用记录、不重置实现）；改单次返回用 `mockResolvedValueOnce`。

---

## V — 验证短板（环境不满足，待补验）

**目标**：补齐 P0–P7 门禁里两个"本机无法验证"的缺口——`-race` 竞态检测、SIGTERM 优雅关闭真机验证。

**2026-09-02 查实结果（已用命令确认，非假设）**：
- **`-race` 本机不可验**：`command -v gcc/cc/clang/cl` 全空；常见 MinGW64/TDM-GCC/MSYS2/LLVM 目录均不存在；`go env CGO_ENABLED=0`、`CC=gcc`；`go test -race` 直接报 `requires cgo; enable cgo by setting CGO_ENABLED=1`。强开 `CGO_ENABLED=1` 也因无 gcc 编译器跑不起来。
- **SIGTERM 本机不可验**：本机是 Windows，Go on Windows 不收 POSIX 的 SIGINT/SIGTERM 做优雅关闭（走 `SetConsoleCtrlHandler`），到不了 `signal.NotifyContext`。需在 Linux 容器用 SIGTERM 真实验证；埋点靠 2s 定时刷盘已能正常落盘，优雅关闭非功能阻塞点。

**结论**：这两项是**环境验证短板，不是功能缺陷**。P0–P7 每一步功能门禁都绿。补验需：装 MinGW-w64 gcc（Windows 跑 `-race`）或 Linux 环境（验 `-race` + SIGTERM）。待合适环境补 `go test -race ./...` 与 SIGTERM 真实关闭验证。

**2026-09-15 补充**：`-race` 已改用 GitHub Actions 的 `ubuntu-latest` runner 补验（`.github/workflows/race.yml`），
push/PR/手动均可触发，可复现且不依赖本机编译器。SIGTERM 仍待 Linux 环境。

---

## G — 版本控制修复 ✅ 已完成（2026-09-15）

**背景**：V1 功能（P0–P8）全闭环，但代码**零提交、无版本控制兜底**。

**查实的四个问题**（实测结论）：
1. **git 根在家目录** `C:/Users/Arina`，项目目录无独立 `.git`。`.git/objects` 达 1.03 GB / 35675 个孤儿对象（index 空、0 提交），`.ssh/id_rsa` 私钥在 git 视野内；`git status` 需扫 3 分 45 秒。
2. **remote 畸形**：`git@github.com:https://github.com/hellotitans/Record_demo_hou.git`（SSH 格式混写 HTTPS 路径）。
3. **本地 0 提交 vs 远端已有 main**（`f40a1b8`，仅一个 83B README）。
4. HTTPS 到 github.com 返回 000（代理拦截）；**SSH 正常**（已认证 `hellotitans`）。

**修复**：
- 家目录 `.git` 重命名为 `.git.bak-20260915`（可还原，1.1G 保留待手动删）。
- 项目目录 `git init -b main`，新增 `.gitignore`（排除 node_modules 123M、server/gocache 130M、编译产物、
  `decisions.json`/`telemetry.jsonl` 等隐私运行时数据、`.workbuddy/`）与 `.gitattributes`（`* text=auto eol=lf`）。
- 首次提交 **92 个文件 / 609K**（不排除依赖与缓存会是 264M）。
- 远端历史用 `git merge --allow-unrelated-histories` 合并保住 README，**全程未用 `--force`**。
- 推送成功 `f40a1b8..0c6ed38`，已建追踪 `main...origin/main`，`git push --dry-run` 报 up-to-date。

**遗留**：`.git.bak-20260915`（1.1G）待确认后手动删除；提交作者邮箱 `houmh1123@163.com` 未与 GitHub 账号关联。

---

## P6+ — V1 其余功能（已全部纳入 P6·a/P6·b/P6·c）

| 原模块 | 现归属 |
|---|---|
| 临界点计算器 | P6·a ✅ |
| 品类模板库 | P6·b ✅ |
| 决策档案 + 回访 | P6·c ✅（P7 起升级为跨设备持久化） |

---

## 本会话验证记录（2026-09-01 复核）

上一会话完成 P0–P5 代码与测试；本会话在新环境中**重新安装 Go 工具链并独立跑通全部门禁 + 真机冒烟**，确认不是"代码在我机器上能跑"的假绿。

**环境**：Go 1.27.0 局部安装于 `C:/Users/Arina/.workbuddy/binaries/go`（不污染系统 PATH）；`GOPATH`/`GOCACHE` 指向仓库内 `server/gopath`、`server/gocache`（均已被 .gitignore 忽略）；模块无第三方依赖，全程离线。

**门禁结果（全绿）**：
```
go build ./...   → BUILD OK
go vet ./...     → VET OK
gofmt -s -l .    → 无输出（格式干净）
go test -cover ./...
  debate        85.8%
  httpsrv       85.6%
  llm           84.8%
  orchestrator  93.5%
  telemetry     89.9%
  cmd/server     0.0%（main 包，无单测，符合预期）
```

**真机冒烟（Mock 模式，未配 API Key，零成本）**：
- `GET /healthz` → `ok`
- `POST /api/debate` 收到完整 SSE 帧序列：`role_assign ×1 → turn_start/delta/turn_end ×8 → moderator ×3 → done ×1`
- 埋点自动落盘 JSONL：**18 行**（`debate_started ×1 + round_delivered ×8 + llm_call_completed ×8 + debate_cost_total ×1`），证明埋点不阻塞主流程且可自动归属

**本会话顺带确认的生产修复**（来自上一会话）：`mock.go` 与 `openai.go` 的 `onDelta` 在 nil 时 panic 的两个隐患已加判空守卫（同时也是生产健壮性改进）。

**仍遗留（非阻塞）**：
- `-race` 竞态检测：本机无 gcc/cgo，跑不了。需装 gcc 或在 Linux 环境下补 `go test -race ./...`。
- 优雅关闭（SIGINT 刷完埋点再退出）：代码实现正确，但 Windows 的 `kill -INT` 走 `SetConsoleCtrlHandler`，到不了 Go 的 `signal.NotifyContext`；需在 Linux 容器用 SIGTERM 真实验证。埋点靠 2s 定时刷盘已能正常落盘。

---

## 每阶段统一的验收命令

> 注意（都是本会话实测踩出来的）：
> 1. Go-on-Windows 的 `GOPATH`/`GOCACHE`/`GOROOT` **必须**用盘符绝对路径（如 `C:/...`），写 `/c/...` 会被当成相对路径报错。
> 2. **不要** `export PATH="$GOROOT/bin:$PATH"` —— git-bash 下 `C:/` 风格的 PATH 不生效，`go` 仍然找不到。直接用全路径调用 `go.exe` 最稳（见下方 `GO=` 一行）。
> 3. `PORT`/`TELEMETRY_FILE`/`DECISIONS_FILE` 等**必须 `export`** 才会进子进程；本地 curl 若走了系统代理，需 `--noproxy '*'` 才能命中本机服务（`--noproxy 127.0.0.1` 对 `localhost` 不生效）。

```bash
export GOROOT="C:/Users/Arina/.workbuddy/binaries/go"
export GOPATH="C:/Users/Arina/WorkBuddy/2026-09-01-12-41-31/server/gopath"
export GOCACHE="C:/Users/Arina/WorkBuddy/2026-09-01-12-41-31/server/gocache"
GO="$GOROOT/bin/go.exe"          # git-bash 下必须用全路径调 go

cd server
"$GO" vet ./...                 # 静态检查
"$GO" build ./...               # 编译
"$GO" test -cover ./...         # 测试 + 覆盖率
"$GO" test -race ./...          # 竞态检测（需先装 gcc/cgo）
"$GOROOT/bin/gofmt" -s -l .     # 格式化（应无输出）
```

前端门禁：

```bash
cd web
npm run typecheck               # tsc --noEmit
npm test -- --run               # vitest
npm run build                   # tsc --noEmit && vite build
```

**一票否决项**：任何一个包编译失败或测试不通过，该阶段不得标记完成。

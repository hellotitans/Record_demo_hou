# 决策辩论产品 — 分阶段开发计划

> 目标：把"数据派 vs 生活派"四段式辩论引擎从零做到可运行、可验证、可观测。
> 原则：**每个阶段有单一目标、清晰边界、独立验收测试**。上一阶段没过测试，不进入下一阶段。
>
> **本文档是「状态索引」版本**（2026-09-16 压缩，486 行 → 约 180 行）。
> P0–P5 的详细验收记录已折叠为一行摘要 —— 完整内容在中后段「已完成阶段档案」与各自的完成报告里。
> 日常开发只需读：阶段总览表 + 阶段边界 + 统一验收命令。改动具体某块时，按 `PROJECT-INDEX.md` 第三节查对应文档。

---

## 阶段总览

| 阶段 | 名称 | 单一目标 | 状态 |
|---|---|---|---|
| **P0** | 埋点底座 | 事件可采集、可落盘、永不阻塞主流程 | ✅ 20 测试 / 89.9% |
| **P1** | 领域模型与提示词 | 数据结构自校验，四段式提示词可复现 | ✅ 16 测试 / 85.8% |
| **P2** | 模型抽象层 | 不联网不花钱跑通全流程；混合模型路由可验证 | ✅ 13 测试 / 84.8% |
| **P3** | 辩论编排引擎 | 四段式流程 + 主持人 + 优雅降级 | ✅ 6 测试 / 93.5% |
| **P4** | HTTP / SSE 交付层 | 端到端可请求，帧序列正确，埋点自动上报 | ✅ httpsrv 85.6% |
| **P5** | 可执行入口与冒烟 | 一条命令起服务，真实 HTTP 请求跑通整场 | ✅ 二进制 + 真机冒烟 |
| **P6** | 前端（React） | 实时辩论流 UI：SSE + 立绘 + 打字机 + 决策卡片 | ✅ 12 测试 |
| **P6·a** | 临界点计算器 | 假设变成可拖拽的"结论翻转沙盘"（纯前端） | ✅ 32 测试 |
| **P6·b** | 品类模板库 | 10 个品类维度预置，点选预填 + 可编辑权重 | ✅ 55 测试 |
| **P6·c** | 决策档案 + 回访 | localStorage 存结论+假设，超 3 月弹回访 | ✅ 72 测试 |
| **P7·a** | 后端 `/api/decisions` | 档案搬到服务端：文件 JSON + REST，跨设备可读 | ✅ store 88.7% |
| **P7·b** | 档案跨设备同步 | 前端"远端权威 + 本地兜底" | ✅ 94 测试 |
| **P8·a** | 后端统计聚合 | store 加 Category + `Stats()` + stats 接口 | ✅ store 92.2% |
| **P8·b** | 后悔率看板 | 总览卡 / 品类排行 / 月度趋势 / 后悔列表 | ✅ 112 测试 |
| **P9** | 真实 LLM 联调 | 接上 DeepSeek，验证真实辩论质量 | ✅ 覆盖率全绿 |
| **P10** | 提示词打磨 | 实测数据驱动的提示词优化 | ✅ orchestrator 94.6% |
| **G** | 版本控制修复 | 项目独立 git 仓库 + GitHub 推送 | ✅ 92 文件 / 609K |
| **V** | 验证短板 | `-race` 竞态检测 / SIGTERM 优雅关闭 | 🔶 `-race` 已由 CI 补验；SIGTERM 待 Linux |

---

## 阶段边界（关键设计约束）

```
debate  ──▶ llm  ──▶ orchestrator  ──▶ httpsrv  ──▶ cmd/server
  │                                        │
  └────────────── telemetry ──────────────┘
```

**依赖方向严格单向，禁止反向依赖。**

| 包 | 职责 | 禁止做的事 |
|---|---|---|
| `debate` | 领域模型 + 提示词 | 任何 I/O、任何对上层的引用 |
| `llm` | 模型调用抽象 | 引用 `debate`（保持通用） |
| `orchestrator` | 流程编排 | 直接写 HTTP、直接写埋点存储 |
| `httpsrv` | 协议层 | 内含业务逻辑 |
| `telemetry` | 采集 | 被主流程阻塞（`Emit` 必须永不阻塞） |
| `store` | 决策档案持久化 | 引用上层 |

**这样做的理由**：每一层都能独立测试。`orchestrator` 用 Mock 模型验证，`httpsrv` 用 Mock 编排器验证 —— 阶段之间互不干扰，某阶段改坏了不会污染其他阶段的测试。

---

## 已完成阶段档案（P0–P5）

> 详细验收记录已折叠。需要看具体断言时，读对应源文件的 `_test.go`。
> 这几段保留的是**仍具约束力的设计判断**，实现细节不再重复。

### P0 — 埋点底座
`server/internal/telemetry/`，零第三方依赖。**核心契约：`Emit` 永不阻塞** —— 缓冲区满直接丢弃并计数（`ErrBufferFull`），因为辩论是 60–120 秒的长任务，宁可丢埋点也不能让用户等。丢弃数通过 `Stats().Dropped` 暴露。
所有事件带 `zone` 字段（`decision` / `execution`）—— 商业内容时序隔离在数据层的落地。
> 详见 `overview.md` 的「Go 埋点模块」段与 `决策辩论产品-埋点设计方案.md`。

### P1 — 领域模型与提示词
**为什么先做这个**：提示词是本产品的核心资产，把它和流程控制分开，才能在不跑模型的前提下审查和迭代。
关键断言：R1 并行的先手无意义（`FirstSpeaker()` 返回 `ok=false`）；R2–R4 先手交替以消除先手优势；四段式提示词每轮含**不同的**战术指令。

### P2 — 模型抽象层
`MockClient` 支持分块流式 + 延迟，能真实模拟取消与超时，`Handler` 并发安全（R1 两方并行调用）。`Calls()` 可断言模型选择 —— 验证"R1 用便宜模型、R2–R4 用强模型"。

### P3 — 辩论编排引擎
**关键设计判断：主体串行，只有 R1 并行。**
质询轮要求 B 点名反驳 A 的发言，存在数据依赖 —— 并行反而会让 B 看不到 A 说了什么。唯一的真实并行点是 R1 立论（该轮明确禁止提及对方）。**不为了用并发而用并发。**

**降级策略（本阶段最重要的部分）**：

| 失败点 | 行为 | 理由 |
|---|---|---|
| 角色分配解析失败 | 降级为确定性分配 + 诚实理由 | 绝不让一场辩论卡在这一步 |
| 主持人输出不合法 | 降级为空笔记，辩论继续 | 让跑了几十秒的辩论因 JSON 少个括号而全盘失败，是把工程问题转嫁给用户 |
| 某一方发言失败 | 保留已完成部分，返回非 nil result | 用户等了 60 秒，半场辩论也比空白页好 |
| 下游（前端）断开 | 立即 `cancel()` | 用户关了页面，不该继续为没人看的辩论付钱 |

主持人笔记中的 `Unresolved` 被喂进下一轮提示词 —— 这是"深度递增"的传动轴。R4 后不再跑主持人，改为提取关键假设。

### P4 — HTTP / SSE 交付层
帧顺序契约：`role_assign → (turn_start → delta* → turn_end) × 8 → moderator × 3 → assumption* → done`。
心跳帧按间隔插入（辩论中途几十秒无数据，没有心跳会被反向代理掐断）。埋点失败绝不影响主流程。

### P5 — 可执行入口与冒烟
配置全部走环境变量（API Key 绝不进代码）。未配置 API Key 时用 Mock 模型启动，本地零成本跑通。

---

## P6 系列 — 前端与 V1 功能

> P6/P6·a/P6·b/P6·c 的完整设计判断与实测数据见各自的完成报告：
> `P6-前端-完成报告.md`、`临界点计算器-完成报告.md`、`品类模板库-完成报告.md`、`决策档案回访-完成报告.md`

| 子阶段 | 一句话 | 关键判断 |
|---|---|---|
| **P6** | 实时辩论流 UI | 原生 `EventSource` 只支持 GET 而 `/api/debate` 是 POST → 用 `fetch + ReadableStream`；`assumption` 帧不单独建 state，随 `done` 帧的 `result.assumptions` 进决策卡片（单一数据源） |
| **P6·a** | 临界点计算器 | 纯函数核心 `lib/critical.ts` 不碰 DOM、不发请求 —— 可单测、可当唯一事实源。滑块默认放在阈值中点，初始即"平局"，**诚实表达"辩论本身不替你做决定"** |
| **P6·b** | 品类模板库 | 后端 `debate.Dilemma` 早已预留 `Category`/`Dimensions`，`prompt.go` 也已渲染进提示词 → 前端发了就端到端生效，**后端零改动**，阶段边界最干净 |
| **P6·c** | 决策档案 + 回访 | 回访是"时间维度上的数据资产"——别的辩论产品抄不走。纯前端用 `createdAt` 判定是否该回访，既拿到核心资产又不引入后端持久化 |

---

## P7 — 决策档案跨设备持久化

> 完整内容：`P7-档案跨设备持久化-完成报告.md`

**范围（用户选定）**：文件 JSON 存储（零第三方依赖）、前端"远端权威 + 本地兜底"、单用户无鉴权。

**仍具约束力的设计判断**：
- 写入用**临时文件 + `os.Rename` 原子替换** —— 进程被杀也不会留下半截 JSON
- **损坏文件 fail-fast**：启动时解析失败直接退出。宁可让人来修，也不能静默清空用户的历史决策
- 错误用哨兵值（`ErrInvalidRecord` / `ErrNotFound`），由 `writeStoreError` 集中映射成 400/404/500 —— 业务语义只在一处翻译
- `POST /api/decisions` 按 `id` 幂等：首次 201、覆盖 200 —— 前端重试不会长出双胞胎
- 前端所有失败（网络/超时/5xx/结构不合法）**一律降级为 `null`，绝不抛异常** —— 档案是锦上添花，它挂了不能让用户在辩论页看到红色报错
- 回访的 `answeredAt` 以**服务端时钟**为准 —— 跨设备场景下只有服务端时钟是大家公认的那一个

**接口**：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/decisions` | 返回 `{"decisions":[...]}`（新→旧；空档案是 `[]` 不是 `null`） |
| POST | `/api/decisions` | 创建/覆盖一条，201 / 200 |
| POST | `/api/decisions/{id}/followup` | body `{"regret":"yes"\|"no"}` |

---

## P8 — 后悔率看板

> 完整内容：`P8-后悔率看板-完成报告.md`

**仍具约束力的设计判断**：
- **后悔率的分母是"已回访数"，不是"总决策数"**。没回答过的人不能被当成"不后悔"，否则档案越攒越多，后悔率会被稀释成一个好看但没意义的数字
- **小样本的品类照样进排行**。1/1 = 100% 看着吓人，但藏起来才是误导；每条都带 `Answered`，前端把样本量摆在比率旁边
- 时间解析失败的记录**归入"未知"桶而不是丢弃**，各分项之和始终等于 `Total`
- 排序必须**稳定**（map 遍历顺序随机，兜底按品类名排序），否则同一份档案会排出不同顺序
- `DecisionRecord.Category` **不做取值白名单** —— 品类模板定义在前端，后端不该被前端的模板清单绑住
- **统计只认后端一份，不在前端用本地档案重算** —— 聚合口径在 Go 侧有单测守着，前端再实现一遍迟早漂移

**接口**：`GET /api/decisions/stats` → `{"stats":{total,answered,pending,regretYes,regretNo,regretRate,byCategory[],byMonth[],regretted[]}}`

---

## P9 — 真实 LLM 联调

> 完整内容：`P9-真实LLM联调-完成报告.md`

接入 DeepSeek 成功，跑通真实辩论并达到可用标准。**暴露 2 个 Mock 阶段不可能发现的缺陷**：

1. **思考模式导致空响应** —— 默认请求把正文写进 `delta.reasoning_content`，`content` 全 null。修复：客户端级 `Thinking` 字段，`LLM_THINKING=disabled`。附带收益：completion token 从 300 降到 11（省 96%）。
2. **中文角色名被静默丢弃** —— 提示词示例写 `"side":"data"`，模型照抄中文「数据派」。修复三层：`debate.ParseSide` 容错 + 提示词补约束 + 编号按保留项连续。

**判断依据**：与其指望模型永远守规矩，不如在入口容错 —— **约定由代码兜住，提示词只做提示**。

**真实成本**：单场 44.8 秒 / 13 次调用 / 约 ¥0.03。⚠️ 网上的 `deepseek-chat` / `deepseek-reasoner` 已不在模型列表，照抄教程会 404。

---

## P10 — 提示词打磨

> 完整内容：`P10-提示词打磨-完成报告.md`（**改提示词前必读**）

基于 P9 实测数据驱动，不是凭感觉调参。**最重要的一条规律**：

> **写了具体数字上限的 R3 守住了（190/149 字），只写"3 句话"这类软要求的 R4 失控了（484/475 字）。**
> 模型对"不超过 3 句话"没有概念 —— 它不知道自己写了几句。字数纪律**必须写成数字**，并说明超标后果。

**修复的真实缺陷**：假设被硬拆成 `概率 < 0%`（模型把"A 的概率低于 B 的概率"这个比较式强行拆成单变量+阈值）。三层修复：R4 提示词写反面案例 + 提取提示词禁止硬拆 + `isPercent()` 代码兜底。

**A/B 实测**：总字数 -32%、时长 -35%、成本估算 ¥0.102→¥0.045、主持人分歧点 -39%。

**A/B 基线数据**（P9 第二场，供后续对比）：

| 环节 | 实测字数 | 当时要求 |
|---|---|---|
| R1 立论 · 数据派 | 1001 | 无约束 |
| R1 立论 · 生活派 | 890 | 无约束 |
| R2 质询 · 数据派 | 622 | 无约束 |
| R2 质询 · 生活派 | 579 | 无约束 |
| R3 承认反击 | 190 / 149 | ≤200 字 ✅ |
| R4 总结假设 | 484 / 475 | "不超过 3 句话" ❌ |

---

## G — 版本控制修复 ✅（2026-09-15）

**背景**：V1 功能全闭环，但代码零提交、无版本控制兜底。

**查实的四个问题**：git 根在家目录（1.03 GB / 35675 个孤儿对象，私钥在视野内）、remote 畸形（SSH 格式混写 HTTPS 路径）、本地 0 提交 vs 远端已有 main、HTTPS 被代理拦截（SSH 正常）。

**修复**：家目录 `.git` 改名 `.git.bak-20260915`（可还原）；项目目录 `git init -b main` + `.gitignore`（排除 node_modules 123M、gocache 130M、隐私运行时数据）+ `.gitattributes`；首次提交 92 文件 / 609K；`merge --allow-unrelated-histories` 保住 README，**全程未用 `--force`**。

**遗留**：`.git.bak-20260915`（1.1G）待确认后手动删除。

---

## V — 验证短板

**`-race` 竞态检测**：本机无 gcc/cc/clang/cl（实测 `command -v` 全空），`CGO_ENABLED=0`，跑不了。
→ **2026-09-15 已解决**：改用 GitHub Actions 的 `ubuntu-latest` runner 补验（`.github/workflows/race.yml`），push/PR/手动均可触发。

**SIGTERM 优雅关闭**：本机是 Windows，Go 不收 POSIX 的 SIGINT/SIGTERM 做优雅关闭（走 `SetConsoleCtrlHandler`），到不了 `signal.NotifyContext`。
→ **仍待 Linux 环境**。埋点靠 2s 定时刷盘已能正常落盘，非功能阻塞点。

**结论**：这两项是**环境验证短板，不是功能缺陷**。

---

## 统一验收命令

> 都是实测踩出来的：
> 1. Go-on-Windows 的 `GOPATH`/`GOCACHE`/`GOROOT` **必须**用盘符绝对路径（`C:/...`），写 `/c/...` 会被当成相对路径报错。
> 2. **不要** `export PATH="$GOROOT/bin:$PATH"` —— git-bash 下 `C:/` 风格的 PATH 不生效。直接用全路径调用 `go.exe` 最稳。
> 3. `PORT`/`TELEMETRY_FILE`/`DECISIONS_FILE` 等**必须 `export`** 才会进子进程；本地 curl 若走系统代理，需 `--noproxy '*'`（`--noproxy 127.0.0.1` 对 `localhost` 不生效）。

```bash
export GOROOT="C:/Users/Arina/.workbuddy/binaries/go"
export GOPATH="C:/Users/Arina/WorkBuddy/2026-09-01-12-41-31/server/gopath"
export GOCACHE="C:/Users/Arina/WorkBuddy/2026-09-01-12-41-31/server/gocache"
GO="$GOROOT/bin/go.exe"          # git-bash 下必须用全路径调 go

cd server
"$GO" vet ./...                 # 静态检查
"$GO" build ./...               # 编译
"$GO" test -cover ./...         # 测试 + 覆盖率
"$GO" test -race ./...          # 竞态检测（本机无 gcc，走 CI）
"$GOROOT/bin/gofmt" -s -l .     # 格式化（应无输出）
```

前端门禁：

```bash
cd web
npm run typecheck               # tsc --noEmit
npm test -- --run               # vitest
npm run build                   # tsc --noEmit && vite build
```

**一票否决项**：任何一个包编译失败或测试不通过，该阶段不得标记完成。

# P6·c 决策档案 + 回访 — 完成报告

> 阶段边界：**纯前端，后端零改动**。用户确认范围＝纯前端·存档 + 回访。

## 做了什么
每场辩论结束时，把结论 + 关键假设自动存进浏览器 localStorage（决策档案）；首页在表单下方展示档案列表；对超过 3 个月且尚未回访的决策，内联弹出"三个月过去了，你后悔吗？"回访卡，用户点"后悔/不后悔"即落库。这是别的辩论产品抄不走的数据资产。

## 交付文件
- `web/src/lib/archive.ts` — 持久化层：`loadArchive`(容错返回 `[]`)、`saveArchive`、`addDecision`(按 `id` 去重、最新在前)、`markFollowup`(置 `regret`+`answeredAt`)、`isDueForFollowup(rec, months=3, now=Date.now())`。
- `web/src/types.ts` — 新增 `DecisionRecord`(含可选 `followup`)。
- `web/src/components/FollowUp.tsx` — 回访卡（问题 + 后悔/不后悔按钮）。
- `web/src/components/DecisionArchive.tsx` — 档案列表：超期未回访内联 FollowUp、已回访显示态、近期只展示。
- `web/src/App.tsx` — `archive` state（挂载即 `loadArchive`）；`done` 帧由 `result.dilemma_id` + 当前 `question`/`labels` 组装记录落库；idle 渲染档案 + 回访回调 `markFollowup`；`reset()` 不清 archive（跨场持久）。

## 边界为什么干净
主动定时推送需要服务端调度，超出本阶段边界。纯前端用 `createdAt` 时间戳在用户回来时判定"是否该回访"（≥3 个月且无回访结果即弹卡），既拿到核心数据资产，又不引入后端持久化——**此阶段完全没碰后端代码**。

## 中途测试（你的纪律）
- `npm run typecheck` → 零错误
- `npm test` → **72 passed**（11 文件）：
  - `archive.test.ts` 9（round-trip / 去重 / markFollowup / isDueForFollowup 含边界与损坏数据降级）
  - `FollowUp.test.tsx` 3、`DecisionArchive.test.tsx` 4（空态 / 超期弹回访并回调 / 近期不弹 / 已回访态）
  - `App.test.tsx` 增 1（**完整流程 done 后 localStorage 可读回该决策记录**的集成断言）
- `npm run build` → 459 模块转译，`dist/` 产出

## 顺手修的测试坑（已写进计划文档供复用）
1. `isDueForFollowup` 的"近期"用例曾误写成 `now - 10*MONTH`（实为 10 个月前、已超期），修正为 10 天前；边界用 `>= months*30d` 表示"满 N 个月即到期"。
2. App 落库测试必须给 `h.frames` 设 `done` 帧，否则 mock 不推帧、永远到不了 `done`、断言超时。
3. `calls.at(-1)` 因 tsconfig 的 lib 未开 es2022 在 typecheck 报错，改用 `calls[calls.length - 1]`。
4. `App.test.tsx` 的 `beforeEach` 加 `localStorage.clear()` 隔离各测试。

## 已登记
`开发计划-分阶段实施.md` 总览表 P6·c ✅、P6+ 表改为"已全部纳入 P6·a/b/c"；新增 P6·c 小节（范围/边界/验收/踩坑）；今日工作日志追加本子阶段。

## 当前状态
P6 全部子阶段（P6·a 临界点计算器 / P6·b 品类模板库 / P6·c 决策档案+回访）均已完成，**V1 前端功能闭环**。

## 后续可选（非阻塞）
- 前端跨设备持久化：加后端存储与 `/api/decisions` 接口（替换 `archive.ts` 单一持久化层）。
- 真实定时回访：需服务端调度（当前为"回来时判定"的纯前端近似）。
- 后端老遗留：`-race`（需 gcc/cgo）、Linux SIGTERM 优雅关闭。

# P8 后悔率看板 — 完成报告

> 把 P7 存下来的"你后悔吗"回访数据，变成一眼能读懂的决策质量视图。
> 分两个子阶段推进，各自独立门禁：**P8·a 后端聚合**（只跑 Go）、**P8·b 前端看板**（只跑前端）。

## 一、做了什么

### 用户确认的范围

| 决策点 | 选择 |
|---|---|
| 是否给档案加 `category` 字段 | **加**（支撑"哪类决策你最容易后悔"） |
| 看板指标 | **四项全要**：总览数字卡 / 按品类后悔率排行 / 月度趋势 / 最后悔的决策列表 |

### 看板长什么样

```
决策复盘   后悔率只统计已经回访过的决策
┌────────┬────────┬────────┬────────┐
│   5    │   3    │  67%   │   1    │
│ 总决策 │ 已回访 │ 后悔率 │ 待回访 │
└────────┴────────┴────────┴────────┘

按品类后悔率                    后悔 / 已回访
  🌆 城市选择            1/1 （1 条）  ████████ 100%
  📱 数码产品购买        1/2 （2 条）  ████     50%
  💼 求职选择            0/0 （1 条）
  rent                  0/0 （1 条）

月度决策与后悔趋势                按月 · 后悔率
  2026-02  ████ 100%     1 条 · 100%
  2026-05  ████          1 条 · 0%
  2026-08  ████████ 50%  2 条 · 50%
  2026-09  ████          1 条 · 0%

最后悔的决策                        2 条
  去哪个城市        2026-02-22 · 🌆 城市选择
  买 iPhone 还是安卓 2026-08-31 · 📱 数码产品购买
```

## 二、关键设计判断（以及为什么）

### 后端

1. **后悔率的分母是"已回访数"，不是"总决策数"**
   没回答过的人不能被当成"不后悔"。否则档案越攒越多，后悔率会被慢慢稀释成一个好看但没有意义的数字。已回访为 0 时返回 `0` 而不是 `NaN`——"后悔率 0%" 和 "没有数据" 是两回事，前端靠 `answered` 区分。

2. **小样本的品类照样进排行，但必须带样本量**
   1/1 = 100% 看着吓人，但把它藏起来才是误导。所以每条都返回 `answered`，前端把 "1/2（2 条）" 摆在比率旁边，由用户自己判断这个 100% 值不值得信。

3. **`ComputeStats` 是纯函数，不碰锁**
   单测可以直接喂一组记录断言结果，不必先造文件、起 `FileStore`。`FileStore.Stats()` 只负责加锁调用。

4. **`category` 不做取值白名单**
   品类模板定义在前端 `templates.ts`，后端不该被前端的模板清单绑住——新增品类无需改后端。老档案（P8 之前落库的）没有该字段，读出即空串，归入"未分类"桶而不是被丢掉。

5. **时间解析失败的记录归入"未知"桶，不丢弃**
   保证各分项之和始终等于 `total`，看板上不会出现"总数对不上"的疑惑。

6. **排序结果必须稳定**
   map 遍历顺序随机，兜底按品类名排序——否则同一份档案会排出不同顺序，测试也就没法断言。

### 前端

7. **统计只认后端一份，不在前端用本地档案重算一遍**
   聚合口径（分母、90 天到期）在 Go 侧有单测守着，前端再实现一遍等于把同一套语义复制成两份，迟早漂移。后端不可用时看板整块不渲染——它是锦上添花，不该反过来成为主流程的负担。

8. **条形图用纯 CSS 宽度，不引图表库**
   最多十来个数据点，图表库带来的是体积和依赖，不是可读性。与项目"零第三方依赖"的纪律一致。

9. **App 落库品类用 ref 抓**
   `start()` 一开头就 `reset()` 清掉了 `category` state，而落库发生在几十秒后的 `done` 帧——不先用 `debateCategoryRef` 存下来，落库时拿到的就是 `null`。这条有专门的测试守着。

## 三、验收结果

### P8·a 后端（Go 门禁全绿）

```
gofmt -s -l .   无输出（格式干净）
go vet ./...    OK
go build ./...  OK
go test -cover ./...
  store        92.2%   （P7 时 88.7%）
  httpsrv      86.8%   （P7 时 86.4%）
  orchestrator 94.1% / telemetry 89.9% / debate 85.8% / llm 84.8%（不变）
```

- `store` 新增 **12 测试**：空档案（切片是 `[]` 不是 `nil`）、分母口径、90 天边界（89/90/200 天 + 已回访超期）、坏时间归未知桶、品类排序与**稳定性（跑 20 次）**、未分类兼容、月度排序含未知桶、后悔列表倒序、Followup 深拷贝、`FileStore.Stats()` 端到端与重开可读回（含 category 落盘）。
- `httpsrv` 新增 **7 测试**：走**与 `cmd/server` 一致的真实 mux**、空档案、HTTP 往返聚合、pending 计数、category 落盘进统计、405（Allow 含 GET）、未注册路径 404。
- **真机冒烟**：起服务造 5 条带品类数据（含 1 条 120 天前未回访）→ `total=5 answered=3 pending=1 regretYes=2 rate=0.6667`，品类排序 `city(1.0) > phone(0.5) > job(0) > rent(0)`，与预期**逐项吻合**。

### P8·b 前端（前端门禁全绿）

```
npm run typecheck  零错误
npm test          112 passed（13 文件；P7 时 94）
npm run build     461 模块转译
```

- `StatsBoard.test.tsx` **10**（null/空档案不渲染、四个总览数字、品类中文名与"未分类"、样本量可见、排序、月度含"未知"、后悔列表、空态）
- `decisionsApi.test.ts` **+5**（URL/方法、500、网络中断、5 种坏结构、无 fetch 环境）
- `App.test.tsx` **+3**（落库带 `category=phone`、未选品类时**不带**该字段、后端返回统计时首页渲染看板）
- **前后端真机联调**：后端 8080 + vite dev 5173，**经 vite proxy** 打 `/api/decisions/stats` 拿到与落盘数据一致的统计。

## 四、新增/修改的文件

**后端**
- `server/internal/store/stats.go`（新）+ `stats_test.go`（新）—— 聚合层
- `server/internal/store/decision.go` —— `DecisionRecord` 加 `Category`
- `server/internal/httpsrv/stats.go`（新）+ `stats_test.go`（新）—— `GET /api/decisions/stats`
- `server/cmd/server/main.go` —— 注册路由

**前端**
- `web/src/types.ts` —— `DecisionRecord` 加 `category?`；新增 `DecisionStats` / `CategoryStat` / `MonthStat`
- `web/src/lib/decisionsApi.ts` —— `fetchStats()`（失败降级 `null`）
- `web/src/components/StatsBoard.tsx`（新）+ 测试
- `web/src/App.tsx` —— 落库带品类、刷新统计、渲染看板

## 五、测试踩坑（已修，供复用）

- 给 `vi.mock('./lib/decisionsApi')` 加新导出后**忘了同步补进 mock，会让所有 App 测试全挂**（报 `No "fetchStats" export is defined on the mock`）。
- `StatsBoard` 里同一个品类名会同时出现在"品类排行"和"最后悔的决策"两处，`getByText` 命中多个 → 测试数据要把 `regretted` 置空或只留一个品类。
- 用 `mockResolvedValue` 改默认实现会**污染后续测试**（`vi.clearAllMocks()` 只清调用记录、不重置实现）→ 单次返回用 `mockResolvedValueOnce`。
- `store` 包测试里已有 `rec(id, question)` 辅助函数，新测试再定义 `rec` 会撞名（改名 `statRec`）。
- 走 mux 时 405 是 **ServeMux 自动生成的**（`Allow: GET, HEAD`，比 handler 手写的更准），断言别写死等于 `"GET"`；handler 自带分支要单独直接调才覆盖得到。

## 六、遗留

- **版本控制是坏的**（非 P8 引入）：`git` 根目录是 `C:/Users/Arina`（整个家目录）、零提交、项目目录没有 `.git`、remote 畸形。**当前代码没有任何版本控制兜底，改坏了无法回滚。**
- `-race`：本机装 gcc 失败（所有二进制源被网络代理封禁）→ 已改用 `.github/workflows/race.yml` 在 Linux runner 上跑，需推送到 GitHub 才生效。
- SIGTERM 优雅关闭：仍需 Linux 环境验证。

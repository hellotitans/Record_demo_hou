# 本地使用指南

> 这份文档回答：**前端做到什么程度了？我怎么在本地实际用？怎么接真实 LLM？**
> 最后更新：2026-09-16

---

## 一、前端做到什么程度

**结论：V1 功能已完整闭环，是一个能实际使用的前端，不是原型。**

9 个组件、5 个纯逻辑模块，约 2500 行 TypeScript（含测试），112 个前端测试全绿。

| 模块 | 文件 | 职责 |
|---|---|---|
| 流式消费 | `lib/sse.ts` | 解析 POST SSE（原生 EventSource 只支持 GET，故用 `fetch + ReadableStream`）；跨块缓冲、坏帧只丢不崩 |
| 临界点计算器 | `lib/critical.ts` + `CriticalCalculator.tsx` | 把辩论假设变成可拖拽滑块，实时看结论在哪一点翻转 |
| 品类模板库 | `lib/templates.ts` + `CategoryPicker.tsx` | 10 个品类（数码/租房买房/求职/考研/城市/投资/婚恋/医疗/创业/买车）点选预填 |
| 维度编辑器 | `DimensionEditor.tsx` | 每条维度可改名、拖权重（0–100%）、删除、新增 |
| 决策档案 | `lib/archive.ts` + `DecisionArchive.tsx` | 每场结论与假设自动存档，跨设备同步 |
| 回访卡 | `FollowUp.tsx` | 超 3 个月的决策弹出"你后悔吗" |
| 后悔率看板 | `StatsBoard.tsx` | 总览卡 / 品类排行 / 月度趋势 / 后悔列表 |
| 辩论流 | `DebateStream.tsx` | 按轮重排 + 打字机光标 |
| 决策卡片 | `DecisionCard.tsx` | 结论卡片，可导出长图（`html-to-image`） |
| 立绘 | `Avatar.tsx` | CSS/SVG 占位头像 + 发言呼吸光环 |

**界面语言**：Tailwind，浅色主题，简洁卡片风。不是最终视觉稿，但结构完整、可直接用。

---

## 二、在本地跑起来（Mock 模式，零成本）

### 启动：双击 `start-mock.bat`

打开项目文件夹 `C:\Users\Arina\WorkBuddy\2026-09-01-12-41-31`，
找到 **`start-mock.bat`**，**双击它**。会弹出两个黑窗口（后端 + 前端），
等约 10 秒，脚本会自己打印一行小结告诉你两个服务是否都起来了：

```
  [OK] Backend is LISTENING on port 8080
  [OK] Frontend is LISTENING on port 5173
```

看到这两行 `[OK]`，再打开浏览器：

```
http://localhost:5173/
```

> ⚠️ **必须用 `localhost`，不要用 `127.0.0.1`。**
> 本机 Vite 只监听 IPv6 的 `[::1]`，用 `127.0.0.1` 会连不上（实测 HTTP 000）。
> 这是本机环境的坑，不是代码问题。

> ⚠️ **不要用 bash 去跑**（`.sh` 版本已作废删除）。
> 本机的 `bash` 会走到 WSL，而 WSL 认不出 `C:/...` 这种 Windows 路径，
> 会报 `go.exe: No such file or directory`，后端根本起不来。

**两个黑窗口不要关**，关掉就等于停止服务。用完直接关窗口即可。

Mock 模式下辩论内容是**占位文本**，用来验证流程与 UI，不是真实论证。想看真实论证往下看第三节。

### 脚本为什么写成这样（改之前请先读）

`.bat` 里的注释**全是 ASCII 英文**，这是有意为之，不要"顺手美化成中文"：

cmd.exe 解析 `.bat` 用的是**系统代码页**（本机 GBK），而文件存的是 UTF-8。
中文注释的多字节序列会被逐字节拆散，行首的 `rem` / `echo` 一起被吃掉，
残留碎片被当成命令执行，于是满屏 `'xxx' 不是内部或外部命令`，脚本直接崩掉。
（2026-09-16 实际发生过一次，用户双击后满屏乱码报错。）

同时 `.bat` **必须用 CRLF 行尾**：裸 LF 会让 cmd 在 `for /f`、`goto :label`、
括号块上定位错乱。仓库的 `.gitattributes` 已加 `*.bat text eol=crlf`，
保证无论是谁、什么时候检出，拿到手的都是 CRLF。

一句话：**`.bat` 只写 ASCII、只用 CRLF。** 中文留给 `.md` 文档和网页界面。

### 为什么不再用 `go run`

`start-mock.bat` 是**先编译成 `server\debate-server.exe` 再运行**，不是 `go run`。这不是多此一举：

`go run` 会让子进程的工作目录落到 Go 的构建临时目录，而埋点文件 `telemetry.jsonl`
用的是相对路径，结果 `os.OpenFile` 报 `Access is denied`，`log.Fatalf` 让进程**秒退**——
现象就是「填完问题点开始辩论，跳到一个空白页」，而且报错一闪而过，极难排查。
（2026-09-16 实测复现并已修复：程序现在会把数据文件路径锚定到 exe 所在目录，
不再依赖工作目录；但编译成 exe 仍然更稳，也能提前暴露编译错误。）


### 界面上可以玩什么

1. **选品类** — 顶部横向滚动 chips，点一个（如「求职」），表单自动预填问题与选项，并载入该品类的决策维度
2. **调维度权重** — 拖滑块改权重，或加/删维度。权重代表"你在乎什么"，会进提示词影响辩论重点
3. **开始辩论** — 两个头像亮起 + 打字机流式输出 + 主持人分歧挑明卡
4. **拖临界点滑块** — 辩论结束后出现，拖动滑块能看到结论在哪一点翻转，初始是"平局"
5. **看决策档案** — 首页往下，每场结论与假设自动存着
6. **后悔率看板** — 有已回访数据后出现（首次使用是空的）

---

## 三、接真实 LLM

**后端早就支持了**，不需要改任何代码，只要把 Key 填进 `key.env`。

### 步骤

**第 1 步**：如果还没有 `key.env`，把 `key.env.example` 复制一份、改名成 `key.env`。
（本项目里 `key.env` **已经存在且已填好**，可以跳过这步。）

**第 2 步**：用记事本打开 `key.env`，只改最后一行引号里的内容：

```
OPENAI_API_KEY="sk-e907ad8da31e406ba96cfc238d2cee63"
             ↑ 引号留着，只换引号里那串
```

- 引号**要留着**
- 等号两边**不要有空格**（不要写成 `OPENAI_API_KEY = "..."`）
- 改完保存

**第 3 步**：回到项目文件夹，**双击 `start-real.bat`**。
它会先打印一行掩码后的 Key（如 `sk-e907****ee63`）让你确认读对了，然后启动。

> ⚠️ 同样**不要用 bash 跑**（`.sh` 版本已作废删除，原因见第二节）。
> ⚠️ `key.env` 已被 `.gitignore` 排除，不会被提交到仓库。

### 成本

| 项 | 实测值（2026-09-16） |
|---|---|
| 单场耗时 | 约 45~60 秒 |
| 单场花费 | 约 **¥0.046** |
| ¥10 可跑 | 约 **200 场** |

调用 13 次：8 次发言 + 3 次主持人 + 1 次假设提取 + 1 次角色分配。
模型：`deepseek-flash`（立论+主持人）/ `deepseek-v4-pro`（质询/反击/总结/假设提取）。

### 手动启动（不想用脚本）

```bat
set OPENAI_API_KEY=sk-你的key
set OPENAI_BASE_URL=https://api.deepseek.com
set MODEL_CHEAP=deepseek-flash
set MODEL_STRONG=deepseek-v4-pro
set LLM_THINKING=disabled
set TELEMETRY_FILE=C:\Users\Arina\WorkBuddy\2026-09-01-12-41-31\server\telemetry.jsonl
set DECISIONS_FILE=C:\Users\Arina\WorkBuddy\2026-09-01-12-41-31\server\decisions.json
cd /d C:\Users\Arina\WorkBuddy\2026-09-01-12-41-31\server
C:\Users\Arina\.workbuddy\binaries\go\bin\go.exe build -o debate-server.exe ./cmd/server
debate-server.exe
```

（`TELEMETRY_FILE` / `DECISIONS_FILE` 建议给绝对路径。不给也行——
程序会自动锚定到 exe 所在目录，但给绝对路径更一目了然。）


### 接其他模型

任何 **OpenAI 兼容**接口都能接，改三个环境变量即可：

| 变量 | 说明 |
|---|---|
| `OPENAI_BASE_URL` | 接口地址（如 `https://api.moonshot.cn/v1`） |
| `MODEL_CHEAP` | 廉价档模型名（承担立论 + 主持人） |
| `MODEL_STRONG` | 强力档模型名（承担质询/反击/总结/假设提取） |
| `PRICE_PROMPT_PER_K` / `PRICE_COMPLETION_PER_K` | 可选，你的模型单价（元/千 token），用于成本估算 |

**两个必须注意的坑**（都踩过）：

1. **`LLM_THINKING=disabled` 不要去掉。** 思考型模型（DeepSeek 全系）默认把正文写进 `reasoning_content`，
   而本服务只读 `content` —— 结果是**整场辩论拿到空响应**。去掉这行会直接坏掉。
2. **模型名会换代。** 网上的 `deepseek-chat` / `deepseek-reasoner` 已不在可用列表，
   照抄教程会 404。填之前先查 `GET /models`。

---

## 四、数据存在哪（隐私相关）

| 数据 | 位置 | 说明 |
|---|---|---|
| 辩论内容 | 只在内存 + 浏览器 | 不做持久化 |
| 决策档案 | `server/decisions.json` | 你主动存档才写；已在 `.gitignore` |
| 埋点 | `server/telemetry.jsonl` | 事件流；已在 `.gitignore` |
| 笔记/草稿 | 浏览器 localStorage | 不上传 |

`decisions.json` 与 `telemetry.jsonl` 都在 `.gitignore` 里，**不会进仓库**。
密钥走 `key.env`（也被忽略）或环境变量，**不进代码**。

---

## 五、当前限制（诚实说明）

| 限制 | 说明 |
|---|---|
| 单用户无鉴权 | 个人工具定位，没有登录系统。**不要直接暴露到公网** |
| 无实时协作 | 同一时刻一个人用 |
| 立绘是占位图 | CSS/SVG 头像，不是美术资源 |
| 视觉非终稿 | 结构完整可用，但不是最终设计稿 |
| R3 字数偶有溢出 | 约 ±15%，已接受，不做硬截断（理由见 P11 报告） |
| 优雅关闭未验证 | Windows 到不了 POSIX 信号，需 Linux 环境 |

---

## 六、排障

| 现象 | 原因 | 解法 |
|---|---|---|
| **点「开始辩论」跳空白页** | **后端没起来**（最常见） | 看 `debate-backend-*` 黑窗口有没有报错。见下方专条 |
| **点「开始辩论」跳空白页** | 前端被挤到 5174，你访问的还是 5173 | 访问前先看前端窗口打印的端口号；`.bat` 已改为占用即报错，不再静默换端口 |
| **双击 `.bat` 满屏「不是内部或外部命令」** | `.bat` 含中文注释（cmd 按 GBK 解析 UTF-8）或裸 LF 行尾 | 已修复。**改 `.bat` 时保持注释为 ASCII 英文**，且别改动行尾 |
| 启动脚本打印 `[!!] Backend is NOT listening` | 后端启动失败 | 看 `debate-backend-*` 窗口的具体报错 |
| 前端打不开 | 用了 `127.0.0.1` | 改用 `localhost` |
| 辩论是乱码/占位文本 | 走的 Mock 模式 | 正常。接真实模型见第三节 |
| 报 `go.exe: No such file or directory` | 用 bash 跑了 `.sh`（走了 WSL） | 双击 `.bat`，不要用 bash |
| 报 `GOPATH entry is relative` | 路径写成 `/c/...` | 必须是 `C:/...` 盘符形式 |
| 报 `打开埋点文件失败: Access is denied` | 用 `go run` 跑，工作目录不可控 | 改用 `start-*.bat`（先编译再运行）；程序也已改为自动锚定 exe 目录 |
| 真实模型返回空内容 | 没关思考模式 | 确认 `LLM_THINKING=disabled` |
| 模型 404 | 模型名过期 | 查 `GET /models` 确认 |
| 档案文件损坏 | 手工改坏了 JSON | 服务会启动失败（有意设计），修好文件再启 |

### 「点开始辩论 → 空白页」怎么自查

按顺序看三件事：

1. **后端窗口还开着吗？** 标题为 `debate-backend-mock` / `debate-backend-real` 的窗口
   如果已经消失，就是后端崩了。窗口里的最后几行报错就是原因。
2. **后端在监听吗？** 打开浏览器访问 `http://127.0.0.1:8080/healthz`，
   看到 `ok` 说明后端正常；连不上就是后端没起来。
3. **前端的端口是 5173 吗？** 前端窗口里 `VITE ready` 那行会写明端口。
   如果是 5174，说明 5173 被别的进程占了，要按提示先清掉。

这三步能区分"后端问题"和"前端端口问题"——本次故障两者同时发生，
所以看起来像前端 bug，实际主因是后端根本没起来。


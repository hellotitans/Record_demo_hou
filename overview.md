# 会话概览

## 第一轮：需求拷问（grill-me）

对「决策辩论网站」创意做了 6 大分支、18 个决策的结构化拷问。

**产物**：`决策辩论产品-需求梳理与评估.md`

**三个关键转向**：
1. 辩论之后必须有收敛 —— 原方案在辩论结束处断掉了。解法不是"给建议"，而是"给量化结果 + 临界点分析"（计算器不是建议），把价值观问题转化成用户能回答的事实判断问题。
2. 弃用天使/恶魔（用户自己发现有道德暗示，与"不给建议"冲突）→ **数据派 vs 生活派**，差异落在思维方式而非价值观。天使/恶魔保留为彩蛋皮肤。
3. R4 暴露的关键假设 → 自动喂给临界点计算器，辩论与量化串成一条龙。

## 第二轮：商业化隔离 + 埋点

**产物**：
- `决策辩论产品-埋点设计方案.md`
- `server/` —— Go 埋点模块（可运行、带测试）

**商业化：从空间隔离升级为时序隔离**

用户的"独立版块"（空间隔离）升级为"决策区无菌 / 执行区开放"（时序隔离）。核心洞察：用户还在纠结时展示商业内容转化最低、污染最重；决策完成后展示转化最高、零污染。
准入红线：可接"执行工具"（贷款/搬家/装修），不可接"决策标的"（房源推荐）。

数据层落地：所有事件带 `zone` 字段，执行区数据单独存储，永不进入决策质量分析。

**埋点：四层 + 显式反馈**

| 层 | 内容 | 核心价值 |
|---|---|---|
| L1 漏斗健康度 | `round_read`、`form_abandoned` | 逐轮阅读衰减曲线 = 辩论质量体温计 |
| L2 辩论质量 | `argument_feedback.verdict` | `repetitive` 直接说明主持人去重失效；`fact_card_rejected.domain` 自动生成检索源黑名单 |
| L3 结果闭环 | `decision_recorded` + `revisit_responded` | 三表 join 回答"什么权重配置事后更容易后悔" |
| L4 成本性能 | `llm_call_completed` | 验证混合模型策略是否省钱不伤质量 |

## Go 埋点模块

`server/internal/telemetry/`，零第三方依赖（纯标准库）。

| 文件 | 职责 |
|---|---|
| `event.go` | 事件 schema、区域定义、四层事件常量、typed props |
| `sink.go` | `Sink` 接口 + `WriterSink`（JSON Lines）+ `MultiSink` |
| `collector.go` | 异步收集器：buffered channel、批量/定时刷盘、优雅关闭 |
| `http.go` | 上报接口，支持单条/数组/包装三种格式 |

**核心设计契约**：`Emit` 永不阻塞。缓冲区满直接丢弃并计数（`ErrBufferFull`），因为辩论是 60–120 秒的长任务，宁可丢埋点也不能让用户等。丢弃数通过 `Stats().Dropped` 暴露监控。

**验证结果**（Go 1.27.0）：
- `go vet ./...` 通过
- `go build ./...` 通过
- `go test -cover ./...` → **89.9% 覆盖率，全部通过**
- `go test -race` 无法运行（本机无 gcc/cgo），改用 `-count=10` 压测时序稳定性

## 环境说明

Go 装在**工作区局部**（`.workbuddy/binaries/go/`），未污染系统 PATH。使用时需设置 `GOROOT` / `GOPATH` / `GOCACHE`。

## 遗留

- 竞态检测未跑（缺 C 编译器）。装 gcc 后应补跑 `go test -race ./...`
- ClickHouse / Kafka Sink 未实现（接口已预留）

# 会话概览

> 本文档记录**创意是怎么长成现在这样的** —— 关键的转向、被否掉的方案、判断的依据。
> 想知道"现在在做什么"读 `PROJECT-INDEX.md`；想知道"为什么是现在这样"读本文档。

---

## 第一轮：需求拷问（grill-me）

对「决策辩论网站」创意做了 6 大分支、18 个决策的结构化拷问。

**产物**：`决策辩论产品-需求梳理与评估.md`

**三个关键转向**：
1. **辩论之后必须有收敛** —— 原方案在辩论结束处断掉了。解法不是"给建议"，而是"给量化结果 + 临界点分析"（计算器不是建议），把价值观问题转化成用户能回答的事实判断问题。
2. **弃用天使/恶魔** —— 用户自己发现这个设定有道德暗示，与"不给建议"的产品定位冲突 → 改为**数据派 vs 生活派**，差异落在思维方式而非价值观。天使/恶魔保留为彩蛋皮肤。
3. **R4 暴露的关键假设自动喂给临界点计算器** —— 辩论与量化串成一条龙，而不是两个孤立功能。

---

## 第二轮：商业化隔离 + 埋点

**产物**：
- `决策辩论产品-埋点设计方案.md`
- `server/internal/telemetry/` —— Go 埋点模块（可运行、带测试）

**商业化：从空间隔离升级为时序隔离**

用户的"独立版块"（空间隔离）升级为"决策区无菌 / 执行区开放"（时序隔离）。核心洞察：**用户还在纠结时展示商业内容转化最低、污染最重；决策完成后展示转化最高、零污染。**

准入红线：可接"执行工具"（贷款/搬家/装修），不可接"决策标的"（房源推荐）。

数据层落地：所有事件带 `zone` 字段，执行区数据单独存储，永不进入决策质量分析。

**埋点：四层 + 显式反馈**

| 层 | 内容 | 核心价值 |
|---|---|---|
| L1 漏斗健康度 | `round_read`、`form_abandoned` | 逐轮阅读衰减曲线 = 辩论质量体温计 |
| L2 辩论质量 | `argument_feedback.verdict` | `repetitive` 直接说明主持人去重失效；`fact_card_rejected.domain` 自动生成检索源黑名单 |
| L3 结果闭环 | `decision_recorded` + `revisit_responded` | 三表 join 回答"什么权重配置事后更容易后悔" |
| L4 成本性能 | `llm_call_completed` | 验证混合模型策略是否省钱不伤质量 |

---

## 第三阶段：把创意做成产品（P0–P10）

创意的部分到这里结束，后面是工程。完整阶段索引见 `开发计划-分阶段实施.md`。

**三个决定了产品形态的技术判断**：

1. **提示词是核心资产，必须与流程控制分离**（P1）
   把 `debate` 包做成"只有数据和字符串、零 I/O"，才能在不跑模型、不花钱的前提下审查和迭代提示词。这是后来 P10 能靠实测数据打磨提示词的前提。

2. **主体串行，只有 R1 并行**（P3）
   质询轮要求 B 点名反驳 A，并行会让 B 看不到 A 说了什么。**不为了用并发而用并发。**

3. **约定由代码兜住，提示词只做提示**（P9 确立，P10 强化）
   P9 发现模型会把 `"side":"data"` 照抄成中文「数据派」，被静默丢弃；P10 又发现模型会把"A 的概率低于 B 的概率"硬拆成 `概率 < 0%`。
   两次都证明：**不能指望模型守规矩，入口必须容错。** 这条对任何接 LLM 的结构化输出都适用。

**产品接上真实模型（P9 → P10）**：
DeepSeek 真实联调跑通，单场约 40 秒 / ¥0.045。P10 用实测数据驱动打磨提示词：总字数 -32%、时长 -35%、主持人分歧点 -39%，并修掉假设被硬拆成无效阈值的缺陷。

---

## 当前状态与后续

见 `PROJECT-INDEX.md` 第二节（状态速览）与第五节（下一步）。

**一句话**：V1 全闭环，已接真实模型并完成一轮提示词打磨。下一步最有价值的是**换 2–3 个品类验证提示词规律是否普遍成立**。

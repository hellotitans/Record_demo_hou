# P10 提示词打磨 — 完成报告

> 目标：基于 P9 真实 LLM 联调暴露的薄弱点，用实测数据驱动打磨提示词。
> 方法：同一选题跑三场真实辩论做 A/B 对比（基线 → 打磨一轮 → 修复后回归），每一项改动都有前后数字支撑。
> 时间：2026-09-16

---

## 一、为什么要打磨：从实测里找问题，而不是凭感觉

P9 第一次让产品接上真实模型，跑通了。但"跑通"不等于"好用"。我把 P9 两场辩论的原始 SSE 流完整解析出来，逐轮统计字数、耗时、内容质量，才看清问题在哪。

### 基线的逐轮实测（P9 第二场）

| 环节 | 实测字数 | 提示词要求 | 判定 |
|---|---|---|---|
| R1 立论 · 数据派 | 1001 | **无约束** | 注水 |
| R1 立论 · 生活派 | 890 | **无约束** | 注水 |
| R2 质询 · 数据派 | 622 | **无约束** | 偏长 |
| R2 质询 · 生活派 | 579 | **无约束** | 偏长 |
| R3 承认反击 · 数据派 | 190 | ≤200 字 | ✅ **守住了** |
| R3 承认反击 · 生活派 | 149 | ≤200 字 | ✅ **守住了** |
| R4 总结假设 · 数据派 | 484 | "不超过 3 句话" | ❌ **被无视** |
| R4 总结假设 · 生活派 | 475 | "不超过 3 句话" | ❌ **被无视** |

这张表里藏着本次打磨**最重要的一条规律**：

> **写了具体数字上限的 R3 守住了（190/149 字），只写"3 句话"这类软要求的 R4 失控了（484/475 字）。**

模型对"不超过 3 句话"没有概念——它不知道自己写了几句。但"不超过 200 字"是可执行的。所以字数纪律**必须写成数字**。

### 顺带查实的两个问题

1. **主持人分歧点过长**：实测每条 83 / 68 字。这东西会被原样塞进下一轮的指令里，写长了既稀释辩手注意力，也浪费 token。
2. **成本估算失真**：估 ¥0.102/场，实际账单约 ¥0.03/场，**高估 3 倍多**——会让人误判这个产品能不能放量。

---

## 二、改了什么

### 1. 给每一轮补上可执行的字数上限

在 `sharedRules` 加一条通用纪律，而不是每轮重复解释：

```
6. 严格遵守本轮给出的字数上限。超过上限的部分会被系统截断，
   用户看到的是半截论证 —— 那比写得短更糟。宁可讲透一个点，不要铺开三个点每个都讲一半。
```

然后逐轮给上限，并**写明为什么**，而不只是下命令：

- R1 立论 ≤600 字 —— "实测中不受约束的立论会写到 1000 字以上，多出来的部分是把同一个论点换个说法重讲一遍"
- R2 质询 ≤450 字 —— "点名一个论点讲透，比泛泛反驳三个更有力"
- R3 保持 ≤200 字（实测有效，不动）
- R4 ≤300 字（第一部分 ≤120、第二部分 ≤150），并明确禁止 Markdown 标记

### 2. 修掉一个会误导用户的真实缺陷：假设被抽成无效阈值

这是打磨过程中**新发现的**，比字数问题严重得多。

打磨后第一场，生活派的假设被抽成了这样：

```
[life] 大厂被动离职概率 < 0%
```

**概率不可能小于 0。** 这个条件恒不成立，进了临界点计算器只会给出"生活派永远错"的误导性结论。

根因：模型在 R4 原文写的是"大厂被动离职的概率**低于**创业公司倒闭的概率"——这是**两个量在比较**。而提取器要求输出 `变量 + 运算符 + 阈值` 三件套，模型硬拆，语义全丢，就成了 `< 0%`。

三层修复（同样贯彻"约定由代码兜住，提示词只做提示"）：

1. **R4 提示词**：要求假设必须是"某一个数 ≥/≤ 某一个阈值"，并把这个比较式作为**反面案例**写进去
2. **提取提示词**：明确禁止把比较式硬拆成单变量，"宁可少一条，也不要一条错的"
3. **代码层兜底**：`isPercent()` 拦截单位是百分比但数值越界（`<=0` 或 `>100`）的假设

### 3. 工程修复

- `stream_options.include_usage` 原先只在 JSON 模式发送，普通发言的 token 统计等于依赖服务端默认行为。改为**一律显式请求**。
- 成本估算按真实账单校准：`0.004/0.012`（OpenAI 量级）→ `0.002/0.008`（DeepSeek 官方标价，不含缓存折扣的最坏情况，宁可高估不要低估），并支持 `PRICE_PROMPT_PER_K` / `PRICE_COMPLETION_PER_K` 环境变量覆盖。

---

## 三、效果：A/B 实测对比

同一选题（32 岁要不要从大厂跳槽去 A 轮创业公司），三场真实辩论：

| 指标 | 基线 | 打磨后 | 变化 |
|---|---|---|---|
| 总字数 | 4390 | 2979 | **-32%** |
| 单场时长 | 60.9s | **39.6s** | **-35%** |
| 输出 token | 3669 | 2404 | -34% |
| 估算成本 | ¥0.102 | ¥0.045 | -56% |
| 主持人分歧点 | 83 字 | 51 字 | -39% |

### 字数是否守住上限（第三场回归）

| 环节 | 字数 / 上限 | 判定 |
|---|---|---|
| R1 立论 · 数据派 | 646 / 600 | 超 46 |
| R1 立论 · 生活派 | 671 / 600 | 超 71 |
| R2 质询 · 数据派 | 502 / 450 | 超 52 |
| R2 质询 · 生活派 | 407 / 450 | ✅ |
| R3 承认反击 | 126 / 135 | ✅ ✅ |
| R4 总结假设 | 232 / 260 | ✅ ✅ |

**诚实说明**：模型对字数上限的遵守度大约是 **±15%**，R1/R2 仍会小幅超出。但相比无约束时的 1001 字，683→646 已经是量级改善。我没有继续收紧上限，也没有在代码层硬截断——理由见下。

### 关键假设质量（这是最重要的验收项）

| | 打磨后第一场（有缺陷） | 第三场（修复后） |
|---|---|---|
| 生活派 | ❌ 大厂被动离职概率 **< 0%** | ✅ 职业空窗期承受时间 **≥ 18 个月** |
| 数据派 | B轮融资概率 ≥ 35% | ✅ 期权兑现价值 **≥ 大厂现金总和的 2 倍** |

两条都是**可计算、可用滑块检验**的有效阈值，临界点计算器能真正用起来。

---

## 四、几个判断，以及为什么这么选

**1. 为什么不在代码层硬截断超长输出？**
截断的伤害（半截论证、语义断裂）大于超 46 字的伤害。软约束把 1001 字压到 646 字已经够了，为了最后那 10% 去冒截断的风险不划算。

**2. 为什么不做"假设必须对齐同一变量"的强约束？**
P9 第一场双方自然收敛到同一个变量「可支撑空窗期 18 个月」，方向相反——那是临界点计算器最理想的输入。但第二场、第三场都没有对齐（赔偿月数 vs 期权倍数、空窗期 vs 期权倍数）。

这说明**对齐是运气，不是设计**。我在 R4 提示词里加了一条**温和引导**（"如果存在你和对方都在赌的同一个数，优先选它"），但没有做成强制——强制会扭曲辩论的自然走向，为了计算器的便利牺牲辩论的真实性不值得。

**3. 为什么成本校准选"官方标价"而不是实测值？**
实测 ¥0.03/场里包含了 DeepSeek 的缓存折扣。用实测值订默认值，一旦缓存未命中就会严重低估；用官方标价（最坏情况）则只会高估不会低估，方向更安全。

---

## 五、验收

- 后端全部门禁：`gofmt -s -l` 干净、`go vet` 无告警、`go build` 通过、`go test -cover` 全绿
- **orchestrator 覆盖率 93.8% → 94.6%**（新增 `TestAssumptionsRejectImpossiblePercent` 锁住概率越界防御）
- 其余包覆盖率不变：debate 86.3% / httpsrv 86.8% / llm 85.1% / store 92.2% / telemetry 89.9%
- 三场真实辩论回归验证，无降级
- 已推送 GitHub（`639534c..3c0063e`），CI 自动触发

## 六、遗留

- **上一场曾出现一次 R2 双方输出逐字相同**（md5 一致，827 字），本次打磨中未能复现（后续两场 R2 内容均正常且立场正确）。已记录，若再出现需要加"同轮双方内容雷同"的检测与重试。
- 字数约束仍有约 15% 溢出，属于模型对数值指令的固有偏差，可接受。
- 提示词质量目前只在一个选题上验证过（跳槽）。换到别的品类（买房、考研、医疗）是否同样成立，需要多跑几个选题才能下结论。

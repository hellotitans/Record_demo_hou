# P7 决策档案跨设备持久化 — 完成报告

> 阶段：P7·a（后端 `/api/decisions`）+ P7·b（前端跨设备同步）
> 日期：2026-09-02　　状态：**两个子阶段门禁全绿**

---

## 一、这个阶段解决什么问题

P6·c 把"你后悔吗"的回访数据存进了浏览器 localStorage —— 数据很值钱，但**换台设备就没了**。

P7 把它搬到服务端：同一份决策档案跨设备可读，同时保证**后端没起、断网、报错时功能不残废**。

### 动手前对齐的三个架构分叉

| 分叉点 | 选择 | 理由 |
|---|---|---|
| 后端存储 | **文件 JSON** | 零第三方依赖，与埋点 JSONL 落盘模式一致；不引 SQLite/cgo，Windows 本机直接跑 |
| 前端读写 | **远端权威 + 本地兜底** | 后端在时跨设备一致，后端不在时功能不残废 |
| 多用户归属 | **单用户无鉴权** | 个人工具定位，不是多租户 SaaS |

---

## 二、阶段边界

切成两个**各自独立门禁**的子阶段（不混着跑）：

```
P7·a  后端 ── 门禁只跑 Go（build / vet / test -cover / gofmt）
P7·b  前端 ── 门禁只跑前端（typecheck / test / build）
P7·c  联调 ── 前后端一起起，经 vite proxy 验证完整链路
```

---

## 三、交付内容

### P7·a 后端（Go，零第三方依赖）

| 文件 | 说明 |
|---|---|
| `server/internal/store/decision.go` | 单文件 JSON 存储层：`List/Get/Add/MarkFollowup`，互斥锁保护 |
| `server/internal/httpsrv/decisions.go` | REST 接口 + 错误集中映射 |
| `server/cmd/server/main.go` | 路由接入 + `DECISIONS_FILE` 环境变量 |

**接口**

| 方法 | 路径 | 行为 |
|---|---|---|
| GET | `/api/decisions` | `{"decisions":[...]}`（新→旧；空档案是 `[]` 不是 `null`） |
| POST | `/api/decisions` | 创建/覆盖一条 → `{"decision":{...}}`，首次 **201**、覆盖 **200** |
| POST | `/api/decisions/{id}/followup` | `{"regret":"yes"\|"no"}` → `{"decision":{...}}` |

**关键设计判断**
- **原子写**：临时文件 + `os.Rename` 替换，进程被杀不留半截 JSON；临时文件必清理（有测试守着）。
- **损坏文件 fail-fast**：启动即报错退出。宁可让人来修，也不静默清空用户历史决策。
- **幂等**：按 `id` 去重，前端重试不会长出双胞胎。
- **服务端补 `createdAt`**：前端漏传时补齐 —— 否则前端 `isDueForFollowup` 解析空串得 `NaN`，变成"永远不弹回访"的静默 bug。
- 路由用 Go 1.22+ 增强 ServeMux（方法 + `{id}` 通配），405 自动带 `Allow`。
- 错误用哨兵值 `ErrInvalidRecord`/`ErrNotFound`，在 `writeStoreError` 一处集中映射 400/404/500。

### P7·b 前端

| 文件 | 说明 |
|---|---|
| `web/src/lib/decisionsApi.ts` | API 层，**所有失败降级为 `null`、绝不抛异常** |
| `web/src/lib/archive.ts` | 升级为同步层（保留原本地函数） |
| `web/src/App.tsx` | 挂载水合 + done 落库 + 回访推送 |

**读写策略**
- 读 `hydrateArchive()`：拉远端 → 成功则以远端为准**并回写本地缓存**；失败退回本地。
- 写 `pushDecision()` / `pushFollowup()`：**先落本地（乐观，后端没起也不丢）**，再推远端。
- 远端回执用 `upsertLocal` **就地替换不改顺序** —— 否则"后端确认一下"就把老决策顶到列表最前，用户看着像凭空冒出一条。
- 回访的 `answeredAt` 以**服务端时钟**为准：跨设备场景下只有它是大家公认的那一个。

---

## 四、门禁结果

### P7·a（Go）
```
go build ./...      → BUILD OK
go vet ./...        → VET OK
gofmt -s -l .       → 无输出

go test -cover ./...
  store          88.7%   ← 新增（16 测试）
  httpsrv        86.4%   ← 较 P4 的 85.6% 提升（新增 14 测试）
  debate         85.8%
  llm            84.8%
  orchestrator   93.5%
  telemetry      89.9%
```

`store` 覆盖：round-trip、去重置顶、缺字段拒绝、回访（含覆盖/未知 id）、**重开能读回**（持久化真实性）、**并发 50 写入不丢且文件仍可解析**、损坏文件 fail-fast、无临时文件残留。

`httpsrv` 覆盖：走**与 cmd/server 一致的真实 mux 路由**，覆盖空列表/创建/幂等/400/413/405、回访往返/404/400、未注册路径。

### P7·b（前端）
```
npm run typecheck   → 零错误
npm test            → 94 passed（12 文件）
npm run build       → 460 模块转译
```

新增：`decisionsApi.test.ts` 13、`archive.test.ts` 增 7、`App.test.tsx` 增 2。

### P7·c 真机联调（经 vite proxy 的完整链路）
起后端 8080 + vite dev 5173，从 **vite 端口**打 `/api/decisions`：

| 步骤 | 结果 |
|---|---|
| GET 空档案 | `{"decisions":[]}` 200 |
| POST 创建 | 201，返回落库记录 |
| GET 回读 | 可见刚创建的记录 |
| POST 回访 | 200，`followup` 落库并在列表可见 |
| 未知 id / 非法 regret / DELETE | 404 / 400 / 405（经 proxy 仍正确） |
| 后端磁盘落盘 | JSON 缩进正确，字段与前端 `DecisionRecord` 完全一致 |

---

## 五、踩过的坑（已写进计划文档供复用）

**环境**
- git-bash 下 `export PATH="$GOROOT/bin:$PATH"` 对 `C:/` 风格路径**不生效**，`go` 仍找不到 → 必须全路径调 `go.exe`。
- curl 走系统代理时 `--noproxy 127.0.0.1` **对 `localhost` 不生效** → 用 `--noproxy '*'`。

**前端测试**
- `vi.fn(async () => ...)` 未声明参数 → `mock.calls[0][0]` 在 typecheck 报 "Tuple type '[]' of length '0' has no element at index '0'"；修法 `vi.fn<typeof fetch>(async () => ...)`。
- `App.test.tsx` 给档案 API 打 mock 后，`beforeEach` 需 `vi.clearAllMocks()`（只清调用记录、不重置实现），否则跨测试累积。

---

## 六、遗留（非阻塞）

- `-race` 竞态检测：本机无 gcc/cgo，跑不了。`store` 的并发安全有测试覆盖，但未经 `-race` 验证。
- Linux SIGTERM 优雅关闭：Windows 的 `kill -INT` 到不了 Go 的 `signal.NotifyContext`，需在 Linux 容器验证。

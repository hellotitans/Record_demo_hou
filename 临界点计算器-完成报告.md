# P6·a 临界点计算器 — 完成报告

> 把辩论暴露的关键假设，变成可拖拽的"结论翻转沙盘"。纯前端，消费 `result.assumptions`，不改后端一行代码。

## 它解决什么

辩论四段式最后一轮会暴露双方各自的"关键假设"（如"房价年涨幅 ≥ 3%")。这些假设本来躺在决策卡片里当摆设——本子阶段把它们变成**可交互的量化沙盘**：

- 每个变量一根滑块，用户把"假设"拖成"现实取值"；
- 实时判定：某方"全部假设仍成立"→ 该方论证站得住；一旦现实越过某条临界线，该方论证崩；
- 结论卡片随之翻转（平局 / 某方胜 / 双双崩塌），并标出"把 X 拖过 Y 时结论可能翻转"。

诚实的产品姿态：辩论本身不替用户做决定，它只把"你到底押了什么前提"摊开；结论翻不翻，取决于用户自己输入的现实。

## 算子语义（对齐后端假设提取约定）

假设即"结论成立的前提线"：`realValue OP threshold` 为真则该方论证成立；越过 `value` 即崩溃。临界值就是 `value` 本身。

| operator | 结论翻转方向 |
|---|---|
| `>=` | 低于阈值时翻转 |
| `>` | ≤ 阈值时翻转 |
| `<=` | 高于阈值时翻转 |
| `<` | ≥ 阈值时翻转 |

## 交付物

| 文件 | 作用 |
|---|---|
| `web/src/lib/critical.ts` | 纯函数核心：单假设判定、单方是否成立、结论归属、变量归并、滑块范围、临界提示、默认值 |
| `web/src/lib/critical.test.ts` | 16 个单测：四算子/边界/平局/翻转/both-broken/同变量共享/range 合法性/flipHint 措辞 |
| `web/src/components/CriticalCalculator.tsx` | 滑块沙盘 UI + framer-motion 结论卡片交叉淡入 + "复原到假设线" |
| `web/src/components/CriticalCalculator.test.tsx` | 4 个组件测试：默认平局、拖滑块翻转结论、复原 |
| `web/src/App.tsx` | `done` 区块、决策卡片之下挂载（仅当假设非空） |

## 门禁结果（全绿）

```
npm run typecheck   → 零错误
npm test            → 32 passed（5 文件）
npm run build       → 453 模块转译，dist/ 产出
```

模块测试已端到端验证"拖滑块→结论翻转"：数据派 `涨幅 ≥ 3%` 拖到 2 → 生活派胜；生活派 `月供 ≤ 40%` 拖到 50 → 数据派胜。

## 测试踩坑（已修，供复用）

1. RTL 对 `<input type=range>` 的 `onChange` 监听的是 `input` 事件，`fireEvent.change` 不触发，滑块测试须用 `fireEvent.input`。
2. 结论卡片里的胜方标签（数据派/生活派）也会出现在每条假设的静态文案中，`getByText` 多匹配 → 断言改用结论卡片里**唯一的 subLine** 文案。
3. AnimatePresence 在 jsdom 下退出动画不结束、旧节点常驻 → 只用"新文案出现"做断言，不断言旧文案消失。

## 下一步（待你拍板）

- **P6·b 品类模板库**：6–10 个高频品类的决策维度预置（接 `Dilemma.Dimensions`）。
- **P6·b 决策档案 + 回访**：三个月后问"你后悔吗"，这是唯一抄不走的数据（接 P0 埋点）。
- 后端老遗留：`-race`（需 gcc/cgo）、Linux SIGTERM 优雅关闭。

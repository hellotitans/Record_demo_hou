# 决策辩论产品 · 数据埋点设计方案

> 配套文档：`决策辩论产品-需求梳理与评估.md`
> 日期：2026-09-01

---

## 零、三条铁律

1. **每个事件必须能回答一个具体问题。** 回答不上来的事件一律不埋。埋了不看 = 拖累性能 + 制造隐私负债。
2. **埋点绝不能阻塞主流程。** 辩论是 60–120 秒的长任务，任何埋点故障都不允许影响它。
3. **决策数据是护城河，也是隐私雷区。** 首付、月供、负债都是敏感个人信息 —— 脱敏、授权、可删除，三件事一开始就要做对。

**核心判断：这个产品的北极星指标不是 DAU，是「一次决策的完成率」和「三个月后的满意度」。**

---

## 一、商业内容的时序隔离（埋点分区的前提）

| 区域 | 范围 | 商业内容 | 数据用途 |
|---|---|---|---|
| **决策区（无菌区）** | 进入 → 决策卡片生成 | 绝对零 | 采集纯净的决策过程数据 |
| **执行区** | 决策卡片之后 | 允许 | 与决策数据物理分表，**不混入训练集** |

**准入红线：可接"执行工具"，不可接"决策标的"。**
- ✅ 可接：银行贷款/利率工具、搬家服务、装修服务
- ❌ 拒绝：房产中介的房源推荐（直接干预用户正在纠结的那个选择）

埋点层面：所有事件带 `zone` 字段（`decision` / `execution`），执行区的用户行为数据**单独存储、不进入决策质量分析**。

---

## 二、四层埋点清单

### Layer 1 · 漏斗健康度（产品有没有跑通）

| 事件 | 关键属性 | 回答的问题 |
|---|---|---|
| `debate_started` | category, entry_source | 有多少人真的开始了 |
| `form_step_completed` | step, filled_count, elapsed_ms | 表单走到哪一步 |
| `form_abandoned` | last_step, filled_fields[] | **哪些字段让人放弃**（太难填？不愿暴露？） |
| `gap_list_shown` | gap_count, gaps[] | 信息缺口清单有没有被看到 |
| `gap_item_resolved` | gap_id, action(fill / search / skip) | 用户愿意授权检索哪些信息 |
| `round_delivered` | round, latency_ms, char_count | 每轮到达耗时 |
| `round_read` | round, dwell_ms, scroll_completion | **哪一屏用户划走了** |
| `assumption_resolved` | assumption_id, source(default / user), value | 用户认了多少默认值 |
| `weight_adjusted` | dimension, from, to | 用户改了哪些权重 |
| `dimension_customized` | action(add / remove), dimension | **品类模板缺什么** |
| `tipping_point_viewed` | variable, threshold | |
| `card_generated` | — | |
| `card_shared` / `card_saved` | channel | **分享 = 最高级别的认可** |
| `deep_calc_paywall_shown` / `_purchased` | price | 付费转化 |

**两个最该盯的指标：**
- `round_read` 的**逐轮衰减曲线** —— R3 如果掉一半，说明"承认 + 前提下反击"这一轮没做好
- `form_abandoned` 的**字段分布** —— "月供承受力"这一栏如果大量放弃，说明要么用户不知道，要么不愿填，表单需要改

---

### Layer 2 · 辩论质量（本产品特有，价值最高）

| 事件 | 关键属性 | 回答的问题 |
|---|---|---|
| `argument_feedback` | round, side, arg_id, **verdict** | 逐条论点的用户评价 |
| `argument_expanded` | arg_id | 哪些论点用户想深挖 |
| `fact_card_clicked` | url, domain | 用户真的去核实来源了吗 |
| `fact_card_rejected` | url, domain, reason | **哪些来源被判定不可信** |
| `debate_helpful_rating` | score(1–5), debate_id | 整体有用度 |
| `debate_missing_info` | free_text | 用户觉得还缺什么 |
| `role_swapped` | — | 用户是否质疑了角色分配 |
| `debate_stopped_early` | round, reason | 中途喊停 |

`argument_feedback.verdict` 的四个取值：`useful` / `weak` / `factually_wrong` / **`repetitive`**

> **`repetitive` 是金矿。** 它直接告诉你主持人的去重机制失效了 —— 比任何技术指标都准，而且是用户亲口说的。

> **`fact_card_rejected.domain` 聚合起来就是一张自动生成的检索源黑名单**，比人工维护的白名单更贴合真实用户判断。

---

### Layer 3 · 结果闭环（护城河所在）

| 事件 | 关键属性 | 回答的问题 |
|---|---|---|
| `decision_recorded` | choice, confidence(1–5), expected_regret | 用户最终选了什么、多确定 |
| `revisit_responded` | debate_id, actual_choice, satisfaction(1–5), regret(bool), reason | 三个月后 |

**关联分析（v2 的杀手锏）：**
把 `weight_adjusted` + `decision_recorded` + `revisit_responded` 三张表 join 起来，你就能回答一个全世界没人能回答的问题：

> **什么样的权重配置，事后更容易后悔？**

这就是个人偏好模型的训练集，也是这个产品唯一真正抄不走的东西。

---

### Layer 4 · 成本与性能（别等账单来了才算）

| 事件 | 关键属性 |
|---|---|
| `llm_call_completed` | round, role, model, latency_ms, prompt_tokens, completion_tokens, cost |
| `debate_cost_total` | debate_id, total_cost, total_latency, model_mix |

**为什么必须埋：** 单次成本 ¥0.3–1.5 只是估算，必须被真实数据校正。而且"混合模型策略"（R1 便宜模型、R2–R4 好模型）到底省不省钱、伤不伤质量，只能靠这张表验证 —— 把 `model` 和 Layer 2 的 `debate_helpful_rating` 关联起来，一眼就能看出来。

---

## 三、显式反馈（比埋点更重要）

埋点只能告诉你"用户做了什么"，不能告诉你"用户想要什么"。三个必做的显式入口：

1. **辩论结束后的 1–5 星 + 一句话** —— "这场辩论对你有没有用？"（必答，但必须极轻）
2. **每条论点旁的「这句没道理」按钮** —— 四个选项：没道理 / 重复 / 事实错误 / 说得太满
3. **最后的「还缺什么信息」自由输入框**

---

## 四、工程约束

| 约束 | 做法 |
|---|---|
| 不阻塞主流程 | 前端 `navigator.sendBeacon` 批量上报；后端进 buffered channel，异步批量落库。**埋点挂了辩论照常跑** |
| 采样策略 | 关键事件全量；`weight_adjusted` 这类高频事件只保留最终值；滚动/鼠标类一律不埋 |
| 隐私 | 财务字段脱敏存储；采集前明确授权；一键删除；未经二次授权不得用于模型训练 |
| Schema 版本化 | 事件结构一定会变，一开始就要有 `schema_version` |
| 生命周期 | 每个事件写明 owner 和"它回答什么问题"；三个月无人查看的事件删除 |

---

## 五、埋完之后怎么用（分析视角）

| 分析 | 数据来源 | 产出 |
|---|---|---|
| 逐轮阅读完成率曲线 | `round_read` | 辩论质量体温计，直接指导提示词迭代 |
| 论点负面反馈聚类 | `argument_feedback` | 提示词调优清单 |
| 被拒来源域名榜 | `fact_card_rejected` | 检索源黑名单（自动生成） |
| 用户自定义维度榜 | `dimension_customized` | 品类模板迭代清单 |
| 权重配置 vs 事后满意度 | 三表 join | 个人偏好模型训练集（v2） |
| 单次成本分布 | `debate_cost_total` | 模型选型与定价依据 |

---

## 六、事件 Schema

```json
{
  "schema_version": 1,
  "event_id": "uuid",
  "event_name": "argument_feedback",
  "ts": "2026-09-01T13:20:41+08:00",
  "zone": "decision",
  "session_id": "uuid",
  "debate_id": "uuid",
  "user_id": "anon_hash",
  "properties": {
    "round": 3,
    "side": "data",
    "argument_id": "r3-data-2",
    "verdict": "repetitive"
  }
}
```

`zone` 字段是时序隔离在数据层的落地：执行区的事件（`zone: "execution"`）单独存储，永不进入决策质量分析。

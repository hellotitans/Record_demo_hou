# P6·b 品类模板库 — 完成报告

> 阶段边界：**纯前端，后端零改动**。用户确认范围＝纯前端 + 维度可编辑。

## 做了什么
把"高频两难开箱即用"做成了一个前端模板库：用户在首页点选品类，表单自动预填问题/选项，并挂上该品类常见的**决策维度（带权重）**；维度可改名、可拖权重、可删、可加。提交时把 `category` + `dimensions` 一起发给后端，后端 `prompt.go` 早已会消费这些维度去调两个角色的论证重点。

## 交付文件
- `web/src/lib/templates.ts` — 10 个品类模板（数码购买 / 租房买房 / 求职 / 考研 / 城市 / 投资 / 婚恋 / 医疗 / 创业 / 买车），每模板 2 个样例选项 + 2–4 个带权重维度。
- `web/src/types.ts` — 新增 `Dimension`(key/label/weight/source) 与 `CategoryTemplate`，与后端 `debate.Dimension` 的 JSON 形状一一对应。
- `web/src/components/CategoryPicker.tsx` — 横向可滚动品类 chips + "自定义"清空项，`aria-pressed` 表达选中。
- `web/src/components/DimensionEditor.tsx` — 受控维度编辑器：改名 / 拖权重(0–100%) / 删除 / 添加(`source:"user"`)。
- `web/src/App.tsx` — `selectCategory` 预填表单并载入维度；`start()` 用 `Record<string,unknown>` 动态挂 `category`(有值时)与 `dimensions`(非空时)；`reset()` 一并清空。

## 边界为什么干净
后端 `debate.Dilemma` 早预留 `Category` / `Dimensions` 字段，`prompt.go:305-311` 已把"用户确认过的决策维度及权重"渲染进提示词（两派据此调重点、但不得替用户改权重）。所以前端把维度发过去就是端到端生效——**此阶段完全没碰后端代码**。

## 中途测试（你的纪律）
- `npm run typecheck` → 零错误
- `npm test` → **55 passed**（8 文件）：
  - `templates.test.ts` 13（品类数 6–10、每模板恰 2 选项、维度≥2、权重∈(0,1]、同模板 key 唯一）
  - `CategoryPicker.test.tsx` 4（渲染全部品类、点击命中模板、自定义→null、aria-pressed）
  - `DimensionEditor.test.tsx` 5（渲染/删除/添加/拖权重更新/空态）
  - `App.test.tsx` 增 1（**选品类后请求体真带 `category` 与 `dimensions`** 的集成断言）
- `npm run build` → 456 模块转译，`dist/` 产出
- **后端真机联调**：起 Go 后端（Mock 模式），`POST /api/debate` 带 `category:"phone"` + 2 条 `dimensions` → 完整帧序列 `role_assign×1 → turn_start/delta/turn_end×8 → moderator×3 → done×1` 正常产出、**无 error 帧**，证明维度字段后端解析无误。

## 顺手修的测试坑（已写进计划文档供复用）
1. `DimensionEditor` 是受控组件，测试里传 `onChange={() => {}}` 空操作时拖滑块不会改显示值——需用带 `useState` 的 Harness 包一层才能验证"拖权重→显示更新"。
2. `App.test.tsx` 里 `vi.mocked(streamDebate).mock.calls` 在同文件测试间**累积**，断言请求体必须用 `calls.at(-1)` 取最后一次，否则会读到上一个测试的 body。

## 已登记
`开发计划-分阶段实施.md` 总览表拆出 P6·b ✅ / P6·c ⬜，新增 P6·b 小节（范围/边界/验收/踩坑）；今日工作日志追加本子阶段。

## 下一步由你挑
- P6·c **决策档案 + 回访**（三个月后问"你后悔吗"，依赖 P0 埋点）
- 后端老遗留：`-race`（需 gcc/cgo）、Linux SIGTERM 优雅关闭
